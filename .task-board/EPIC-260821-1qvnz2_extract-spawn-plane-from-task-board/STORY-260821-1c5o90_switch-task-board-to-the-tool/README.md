# STORY-260821-1c5o90: switch-task-board-to-the-tool

## Description
Make task-board consume agents-management for spawn decisions and launches, deleting its own copies of the moved code. Acceptance is observable: task-board's spawn surface (CLI flags, refusal texts, preflight projections, spawn behaviour end to end) is unchanged for an operator; its full suite stays green modulo the known-red set; the board keeps roles, tasks, CRs, worktree isolation and review routing untouched.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
