# STORY-260821-3b4ewr: port-vendor-plugins-and-registries

## Description
Move the vendor layer: model registries with per-model Runtime, capability ranking (evidence-based, never policy), reasoning-effort vocabularies and recommended efforts, per-model usage descriptions, and the runtime registry with its frozen six IDs plus declared cross-runtimes (qwen-codex). Admitted-pair digests must be byte-stable over real configs including task-board's own; the digest is a frozen compatibility surface.

## Scope
(define story scope)

## Acceptance Criteria
(define acceptance criteria)
