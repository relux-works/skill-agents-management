# Flight Logbook

> Institutional memory. Concise, factual, high-signal.
> Newest entries first. One block per insight.

## 2026-08-30

### 0525 — Plugin-graph refusal inventory is mechanical
- FINDING: The v0.4.1 outcome correctly listed nine killed mutants; its 0439 logbook entry incorrectly said eight. `docs/refusal-proof-matrix.md` supersedes both partial counts with 35 enumerated error/call-site rows.
- FIX: `.scripts/verify-refusal-matrix.py` narrows every row in an isolated copy and requires the named production-entry test to exit `1`; build failures and wrong-test failures do not count.
- STATUS: Repeated dependency and explicit-node empty-binary survivors now have direct owner tests. `pkg/plugin/registry_test.go`, `pkg/agentic/multinode_test.go`.

### 0525 — Typed-nil vendor bypassed ErrNilVendor
- ROOT CAUSE: `vendorplugin.Registry.Register` checked only `vendor == nil`; a typed-nil pointer inside the `Vendor` interface reached `ID()` and panicked.
- FIX: `nilVendor` now refuses every nil-capable reflected kind before plugin methods run. `pkg/vendorplugin/registry.go`.
- TEST: Narrowing the gate back to interface-nil only makes `TestRegisterRefusesTypedNilVendor` fail through `Registry.Register`.

### 0439 — A refusal exemplar did not prove the refusal class
- ROOT CAUSE: The v0.4.0 graph tests named one two-node cycle, one first-edge missing dependency, one kind pair and one narrower shadow; compile-clean gates restricted to those subsets stayed green, including same-kind duplicate overwrite and multi-node self-cycle admission.
- FIX: Production-entry negatives now cover same-kind duplicates across sequential/batch registration, self and multi-hop cycles, later-edge missing/kind mismatch, non-empty atomic batches, equal-width shadow divergence, implicit-primary/later-edge plan dependencies, and compatibility adapter atomicity. `pkg/{plugin,agentic,vendorplugin}/*_test.go`.
- TEST: Eight narrowed mutants independently returned exit 1 under named `-count=1` tests; production files were restored and the unmutated targeted suite returned exit 0.
- DECISION: `v0.4.1` is a test-strengthening patch over v0.4.0. Rollback remains a consumer pin to `v0.3.0`; no runtime, board or limit-state data migrates.

### 0340 — Same kind is not the same dependency declaration
- ROOT CAUSE: Vendor graph sync initially accepted an existing agentic node when only `ID` and `Kind` matched; a caller could pre-seed a shadow node that dropped the real system's engine dependency.
- FIX: `vendorplugin.Registry.Register` now requires the exact source declaration before reusing a synced node and refuses mismatch as `plugin.ErrUnsatisfiableDeclaration`. `pkg/vendorplugin/registry.go`.
- TEST: Production registration refuses a same-id/same-kind Pangolin shadow with the `llama-cpp` dependency narrowed away. `pkg/vendorplugin/registry_test.go`.

### 0326 — Plugin direction belongs to declarations, not registry kinds
- ROOT CAUSE: `vendorplugin.Registry.Register` encoded vendor→system as the only legal edge, while `agentic.Plan` could represent only one process; Process-B needed a third package position and a consumer-side shadow plan.
- DECISION: `pkg/plugin.Registry` treats normalized kind and dependencies as opaque data, validates batches atomically, and resolves dependency-first without a kind switch. Existing System/Vendor registries are compatibility adapters; vendor→system semantics remain intact.
- FIX: Added `pkg/plugin`, first new kind `pkg/inferenceengine`, and `agentic.BuildMultiNodePlan`; missing dependencies, kind mismatch, graph/plan cycles, and invalid nodes refuse at production registration/plan entry points.
- DECISION: Release v0.4.0 before task-board's deliberate native-graph migration. Rollback is a consumer pin to v0.3.0; no persisted data migration or tag rewrite.

## 2026-08-29

### 1907 — Partial restart-status cohorts fail closed
- ROOT CAUSE: The decoder validated lifecycle fields independently, so omission from a recognizable current response could be laundered into legacy absence and reach `Healthy`. `pkg/localruntime/decode.go`.
- FIX: One cohort gate now accepts only zero lifecycle fields, the complete four-field pre-deadline fixture, or the complete six-field current fixture; `manual_quarantine` is type-checked and mirrored for wire integrity.
- TEST: Removing each lifecycle field independently now yields `Unknown` with read-failure evidence through `CLIStatusReader` and production `vendorplugin.CheckAvailability`. `pkg/vendorplugin/vendors/local-models/availability_test.go`.

### 1834 — Restart status absence remains distinct from explicit null
- DECISION: A legacy status response missing `restart_not_before` maps to checked `Unknown`; only a present future deadline maps to `Limited`, while present `null` or an elapsed deadline continues through attested broker facts. `pkg/vendorplugin/vendors/local-models/availability.go`.
- TEST: Case 27b drives pre/post fixtures through `localruntime.CLIStatusReader` and production `vendorplugin.CheckAvailability`; disabling the presence gate makes the named legacy-fixture test fail by returning false `Healthy`.
- FINDING: `restart_count` and `half_open` remain historical lifecycle evidence and never mint an active backoff verdict.

### 1834 — JSON null bypasses scalar type checks unless rejected explicitly
- ROOT CAUSE: Go `encoding/json` accepts JSON `null` into non-pointer `int` and `bool` destinations without error, leaving zero values; `Unmarshal` alone therefore does not enforce the restart-extension presence/type matrix.
- FIX: Present `null` now refuses for `restart_count` and `half_open`; nullable timestamp fields retain `null` as their defined no-deadline/no-observation value. `pkg/localruntime/decode.go`.
- TEST: Extension wrong-type cases pin null, malformed timestamps, negative counts, and string-encoded scalars as `ErrDecodeFailure`. `pkg/localruntime/decode_test.go`.

### 1744 — Pricing authority refuses IEEE-754 non-finite values
- ROOT CAUSE: `Pricing.Validate` used ordinary `<`/`>=` comparisons; those admit `NaN`, while infinities could pass the non-negative list-price gate and make exact system-only redeclaration unstable.
- FIX: The canonical validator now refuses `NaN`, `+Inf`, and `-Inf` for list and promotional prices before ordering checks. `pkg/vendorplugin/vendor.go`.
- TEST: Both vendor registration and declaration-owned system-only storage paths reject non-finite pricing; finite cloned authority repeats idempotently. `pkg/vendorplugin/{boardfields_test.go,runtime_test.go}`.
- DECISION: Invalid NaNs are never made semantically equal; validation excludes them before storage or authority comparison.

### 1726 — System-only idempotency includes complete model authority
- ROOT CAUSE: `DeclareRuntime` treated equal `(ID,System,Vendor)` as idempotent even when a vendor-unresolved declaration changed `Models`; the losing caller received `nil` although `BuildLaunch` continued using the first declaration-owned launch authority.
- FIX: System-only redeclarations compare every `Model` field after sorting copied top-level rows by model ID; non-equivalent authority returns `ErrRuntimeConflict` without mutating the winner. `pkg/vendorplugin/{runtime.go,registry.go}`.
- DECISION: Top-level model row order is presentation-only because identity and rank are explicit; ordering inside a row remains part of its published value. Established-vendor redeclaration semantics remain unchanged.
- TEST: Field mutations, reordered equivalent rows, and 24 concurrent conflicting effort authorities drive `DeclareRuntime`; exactly one contender succeeds and subsequent `BuildLaunch` admits only the winner's vocabulary. `pkg/vendorplugin/runtime_test.go`.

### 1601 — System-only runtime is a launch binding, not a fabricated vendor
- ROOT CAUSE: `vendorplugin.BuildLaunch` always called `Registry.ResolveRuntime` and `Vendor.Spawn`; Muse's frozen declaration correctly records an unresolved broker, so the generic consumer migration could launch five frozen runtimes but refused Muse unless it added a forbidden runtime-specific fallback.
- FIX: `BuildLaunch` now resolves one private generic launch binding: established-vendor declarations retain `Vendor.Spawn` plus fidelity checks; vendor-unresolved declarations with validated declaration-owned models use the same lossless passthrough projection directly into their registered system. `pkg/vendorplugin/{spawn.go,plugin.go,runtime.go}`.
- TEST: One production-entry matrix drives all six frozen runtimes through `BuildLaunch`; declaration-owned unknown model, missing effort, and forged effort each refuse; typed `RunContext` survives the system-only path; an unresolved declaration with no model rows still returns `ErrRuntimeVendorUnresolved`. `pkg/vendorplugin/{admissionpin_test.go,spawn_test.go,runtime_test.go}`.
- DECISION: `Registry.ResolveRuntime` remains the strict full-pair API and never returns a `Runtime` with a nil vendor. The broader system-only materialization is private to `BuildLaunch`, generic by declaration shape, and contains no Muse ID branch.
- STATUS: Component-side coordinated blocker resolved; end-to-end production reachability still requires the exact `TASK-260828-3hultd` consumer candidate.

### 1533 — Vendor launch migration was lossy for tracked-run identity
- ROOT CAUSE: `vendorplugin.SpawnRequest` omitted `agentic.RunContext`, and `PassthroughLaunch` therefore returned `LaunchRequest.Run == zero`; migrating the consumer from `agentic.BuildPlan` to `BuildLaunch` would make every system's `WithRunContext` remove run/task/board/context environment identity.
- FIX: Added typed `SpawnRequest.Run`, copied it in `PassthroughLaunch` (and therefore `local-models.Spawn`), and made `checkLaunchFidelity` refuse any changed or dropped run context. `pkg/vendorplugin/{spawn.go,plugin.go}`.
- TEST: `BuildLaunch` now proves all four exported identities reach the child; four independent vendor-redirection cases mutate `RunID`, `TaskID`, `BoardDir`, or drop `ContextID`, and each must refuse before `BuildPlan`. `pkg/vendorplugin/{spawn_test.go,double_test.go}`.
- DECISION: Rejected consumer-side `Env` injection; agentic systems remain the single owner of `WithRunContext` filtering/replacement.
- STATUS: Component contract blocker from `TASK-260828-3hultd` resolved in the revision-3 candidate; coordinated consumer migration may resume against the exact CR tree.

### 1520 — TASK-260829-188jcf rework revision 2: a real bug and a misapplied scope demand, in the same review
- ROOT CAUSE (rev1 CR review, finding 2, high): `Registry.NoteUnregistered` (`pkg/vendorplugin/registry.go`) validated only that `RegistrationDiagnostic.Reason` was non-blank — an unrecognized reason string, or `Reason:"malformed"` with a nil `Err`, was accepted and persisted, letting a caller-forged diagnostic silently degrade the typed `ErrRuntimeConfigMalformed` refusal to unknown-reason noise, or manufacture that refusal with no underlying cause at all.
- FIX: `NoteUnregistered` now requires `Reason == "malformed"` (the only value v1 defines) AND `Err != nil`, refusing otherwise with zero state change. Negative tests added (`internal/regress/localmodels_conditional_registration_test.go`, subtests ix/x/xi) proving both forged shapes are rejected and that a refused diagnostic leaves the id fully unpoisoned — a later valid `DeclareRuntime`/`ResolveRuntime` for the same id behaves as if the forged note never happened.
- FINDING (rev1 CR review, finding 1, release blocker, contested): the review demanded proving `vendorplugin.BuildLaunch`/`pi` registration is reachable from `skill-project-management`'s real `buildLaunchPlan` call site. True as stated (zero production callers, confirmed by a fresh grep) but NOT this task's obligation: architecture decision revision 11 §6 slice 9 says verbatim "this task does not write" that migration, assigns it entirely to `TASK-260828-3hultd`, and §7.1.4 limits `TASK-260829-188jcf`'s own Definition of Done to the producer-side component alone — the M1 *milestone* (not this task's DoD) needs both tasks together. The parity inventory (§17.2) independently records the zero-caller fact as already-known and expected, not something this candidate introduced. This module's own pre-existing `docs/shipped-state.md` §6 already documents that NO system plugin here is imported by any binary this repo ships — `skill-project-management` builds its own per-launch registry via `launchSystemConstructors`, never through `agentic.Default`; Pi is in the exact same position as every existing system (`claude`, `codex`, `gemini`, `muse`, `qwen`, `agy`), not uniquely broken.
- FINDING: cross-checked live on `skill-project-management`'s own board — `TASK-260828-3hultd` is already spawned and `blocked`, its own blocker evidence naming `TASK-260829-188jcf` ("still in development ... no versioned launch/preflight command") as one of two prerequisites, the other being an unrelated 67-commits-behind Story-worktree issue in that repo entirely outside this task's authority. This is independent confirmation the sequencing is real, not an excuse invented here.
- DECISION: did not edit `skill-project-management` from inside this task — different repository, different board, a task already owned and in-flight there, no worktree or authorization held in this session. Addressed finding 1 by citing the architecture's own scope split in the CR response rather than forcing a cross-repo change.
- FIX (rev1 CR review, finding 3, medium): `README.md`'s "Layer 2: pkg/vendorplugin" section still said the local-model resource plane was "DESIGN ONLY" and "there is no local-models package anywhere in this module" — false as of this story, rewritten. `README.md`'s CLI-surface list was missing `agents-management local-runtime status [--json]`, added. `docs/shipped-state.md` §6 said "tools/agents-management imports no plugin package" — no longer literally true (`local-models` is imported directly for the status command); corrected to say no *self-registering* plugin package, since `local-models` deliberately never self-registers and importing it still does not populate `vendorplugin.Default`.
- STATUS: rework complete, `go build/vet/test -race`, `gofmt`, `make build/regress` all green (551 subtests, 0 failures). Handed back to review.
- SCOPE: `pkg/vendorplugin/registry.go`, `internal/regress/localmodels_conditional_registration_test.go`, `README.md`, `docs/shipped-state.md`. No production code touched in `skill-project-management` or `relux-agents-infra`; both boards read-only this revision.

### 1345 — TASK-260829-188jcf M1 implementation: single-source guard caught a shadow RuntimeID table, smoke-golden guard forced a test-isolation fix
- FINDING: first cut of `pkg/vendorplugin/vendors/local-models.Config` stored declared runtimes as `map[vendorplugin.RuntimeID]RuntimeEntry`. `pkg/agentic/singlesource_guard_test.go` (module-wide `go test ./...`) correctly failed the build: any second binding table keyed by `RuntimeID` outside `pkg/vendorplugin/registry.go` is exactly invariant 5's shadow-table failure, however local-only its scope looked.
- FIX: `Config.Runtimes` is a slice (`RuntimeEntry.ID` as a field), scanned linearly — mirrors `Model.Systems []agentic.SystemID`'s existing pattern in this module. `pkg/vendorplugin/vendors/local-models/config.go`, `models.go`.
- FINDING: `internal/regress` blank-imports every Layer-1/Layer-2 plugin, and its own `TestEveryLayerOneSystemHasASmokeCase` requires a captured parity golden for every agentic system reachable in that one test binary. Pi's turn-argument wire protocol is a named, unresolved open item (blocked on a pinned `earendil-works/pi` fixture, adversarial plan case 17) with no golden and no honest way to fabricate one yet — importing the real `pi` package into `internal/regress` at all trips that guard the moment its `init()` registers "pi" into the shared `agentic.Default`.
- FIX: kept `internal/regress`'s own registry-diagnostic tests on a fully isolated `agentic.Registry` (a local fake system under id `"pi"`, never touching `agentic.Default`); moved the real-pi-plus-real-local-models end-to-end `BuildLaunch`/Preflight seam tests into `pkg/vendorplugin/vendors/local-models`'s own suite, which carries no cross-cutting smoke-golden guard.
- DECISION: pi's `Argv`/`Stdin` build only the architecture-pinned prefix (`["pi","--profile",<profile>,"--"]`, resolved binary `agents-infra`) plus the existing precedented PromptPath/Prompt stdin fallback — no turn-argument grammar invented ahead of the pinned external contract case 17 requires.
- STATUS: M1 operative completion set (checklist items 3,4,6,7,8) implemented and tested; `go build/vet/test -race`, `gofmt`, and `make build/vet/test/regress` all green. Handed to review.
- SCOPE: `pkg/agentic/preflight.go` (new), `pkg/agentic/system.go`, `pkg/vendorplugin/{vendor,spawn,registry,plugin}.go`, `pkg/localruntime/` (new), `pkg/vendorplugin/vendors/local-models/` (new), `pkg/agentic/systems/pi/` (new), `tools/agents-management/cmd/localruntime.go` (new), `internal/regress/{localmodels_conditional_registration,availabilityquery_runtime}_test.go` (new), `docs/{architecture,shipped-state,consuming-the-module}.md`. No live model process, no real `agents-infra` binary, no real broker interacted with anywhere in the suite.

### 1159 — Local-runtime architecture revision 9: registration, status vocabulary, and cancellation were each half-fixed
- ROOT CAUSE (independent revision-8 review, five findings): (1) `Registry.Register` unconditionally refuses `len(vendor.Models())==0` — revision 8's "register unconditionally, `Models()` returns nil on an absent machine" design cannot survive that call, and would have poisoned registry construction for every unrelated agent type on a machine with no local model configured. (2) Every revision through 8 recovered `Broker.State`'s closed set by grepping `setState` call sites, which surfaced only the broker's LIVE self-report values (`starting`/`serving`/`lingering`/`draining`) and missed three more values (`absent`/`starting-unverified`/`unverified-stale`) the operator-side status function synthesizes when no live connection can be made — so revision 8's Availability table could map an unverified, possibly-stale RECORD straight to `Healthy`, a capability claim with no live evidence behind it. (3) `agentic.LaunchRequest` has no `RuntimeID`-shaped field at all, so `pi`'s `Preflight` could only ever construct a status query from `Model.ID` — one shared `pi` system instance could not distinguish two runtimes sharing a vendor and model. (4) Preflight's own admit/refuse table ADMITTED on its own read failure (subprocess error, timeout, malformed JSON) — the exact bypass the finding named: a failed read is not the same fact as a successful read reporting "nothing running yet." (5) `TASK-260829-188jcf`'s own checklist still embedded the M2 status-consumer obligation as one of its own items, blocked on unscheduled cross-repo work — so the task could never close on M1 alone, contradicting the document's own claim that M1 and M2 are independently schedulable.
- FIX: registration decision moved out of `Vendor.Models()` (left untouched, still call-stable) into the CALLER (`launchRegistry`): `local-models`/`local-qwen` register/declare CONDITIONALLY, only when `local-models.toml` is present and valid; absent and malformed both skip registration (keeping every unrelated runtime launchable) but are surfaced DIFFERENTLY — malformed gets a loud, distinct diagnostic, absent gets none. A genuinely-empty-but-valid catalog still hits `Register`'s existing `ErrNoModels` refusal, unchanged.
- FIX: full direct read of `pi_shared_operator_darwin.go` (not a partial grep) recovered the real, closed, eleven-pair `(BrokerState, BrokerSource)` vocabulary. Both the Availability/UX table and Preflight's admit/refuse table now switch on the PAIR, not `State` alone — `Healthy` is reachable only through `BrokerSource=="attested"`, never through an unverified last-known record.
- FIX: `agentic.LaunchRequest` gains one field, `Runtime string` (opaque, mirroring `Model.ID`'s own type, to avoid an import cycle with `vendorplugin`), set uniformly by `BuildLaunch` for every runtime immediately after `Vendor.Spawn` returns — never by an individual vendor. `StatusQuery` replaces the bare `Status(ctx, ModelID)` signature, carrying `Runtime`/`Model`/`AgentsInfraProject`/`AgentsInfraProfile`, all four sourced from that same resolved `LaunchRequest`.
- FIX: Preflight's read-failure row reversed from ADMIT to REFUSE. A tenth closed gate (A10) specifies deriving a real, cancellable `context.Context` from `spawn.Config.Abort` BEFORE `BuildCommand` is called, instead of deferring that derivation to "later" as revision 8 did — closes the cancellation half of the same finding.
- DECISION: created `TASK-260829-1kpj01` (M2 status-consumer widening) as its own schedulable task, moved the obligation off `TASK-260829-188jcf` via a new checklist item voiding the two stale M2 items (the board mutation vocabulary has no checklist-item-delete, so they remain as superseded wording history — the same pattern the review itself already accepted for the `LaunchFactContributor` item), and linked the soak task `TASK-260829-lx3o55` `blocked_by` the new task in addition to `TASK-260829-188jcf`.
- STATUS: revision 9 published (empty-tree CR, unchanged-tree validation: vet/build/test/regress all exit 0), handed to independent review.
- SCOPE: `.task-board` outcome resources on `TASK-260829-3jlxed`, plus one new task (`TASK-260829-1kpj01`) and two link/checklist mutations on this module's own board. No production code touched in any of the three repositories. No live local-model process interacted with. `relux-agents-infra`'s and `skill-project-management`'s boards read-only.

### 1113 — Local-runtime architecture revision 8: the `Vendor` contract itself couldn't carry what six revisions assumed
- ROOT CAUSE (independent revision-7 review): `Vendor.Models()` takes zero arguments and is contractually required not to vary between calls (`Registry.Register` calls it exactly once, permanently admits the snapshot) — every revision through 7 designed a `loadConfig(projectRoot)` loader with mtime-driven cache invalidation that this same interface makes impossible to implement. `AvailabilityQuery` carries only `Model`/`Home`, no `RuntimeID` — so a vendor serving more than one declared runtime (which `local-qwen` becomes the moment a second local profile exists) cannot tell which one an `Availability` call is about.
- FIX: deleted the project-local tier from `local-models.toml` entirely — it's global-only now, because the pointer it stores (which local `agents-infra` project/profile owns the weights on this machine) is a machine-level fact, not a per-calling-Story-worktree one. That single change removes "project root" as a fact any `Vendor` method needs, which is what makes `Models()`'s call-stability honestly satisfiable (lazy, memoized forever, exactly like every other vendor's static catalog). `AvailabilityQuery` gains one field, `Runtime RuntimeID` — the identical fix this codebase already shipped once for `pkg/providerlimits.VerdictQuery`, built for the exact same problem (`google` already serves both `gemini` and `agy` under one `VendorID`).
- FINDING: task-board's real closed-gate surface is nine items, not the four the review named — critically `modelDisplayOrder` PANICS THE ENTIRE BINARY at `init()` if any vendor-declared model isn't in a hand-written display-order list (not a per-launch refusal — a boot-time crash), and `pkg/remoteconfig/reasoningeffort` deliberately carries zero imports beyond stdlib to avoid a cycle with the model registry, so it can't just import `vendorplugin` to check a model's reasoning-effort requirement generically.
- FINDING: task-board's tracked spawn run executes inside a detached background OS process reached only via a manifest-file handoff — no `context.Context` from the CLI invocation can ever cross that boundary, structurally, not by oversight. The codebase's own two existing "shell out with a bounded timeout" call sites (`launch_composition.go`, `agy_preflight.go`) already accept this and always locally re-wrap whatever `ctx` they're handed in `context.WithTimeout` — revision 8 adopts that exact precedent for `BuildLaunch`'s new `Preflight` step rather than pretending real upstream cancellation exists.
- DECISION: split Preflight's admit/refuse table from Availability's UX-facing mapping. A positively-absent broker (nobody has cold-started Process B yet — the ordinary first-launch case) is `Unreachable` for UX purposes but explicitly ADMITS for Preflight — reusing one table for both questions was revision 7's actual bug, not a wording problem.
- STATUS: revision 8 published (empty-tree CR, unchanged-tree validation: vet/build/test/regress all exit 0), handed to independent review. `TASK-260829-188jcf` checklist items 1-2 (stale `LaunchFactContributor` wording, found still live on the board by the review) superseded via new items 3-5 rather than deleted — this task-board version has no checklist-item-edit mutation.
- SCOPE: `.task-board` outcome resources on `TASK-260829-3jlxed` only. No production code touched in any of the three repositories. No live local-model process interacted with. `relux-agents-infra`'s and `skill-project-management`'s boards read-only.

### 0730 — Local-runtime identity design: split the turn from the server, and don't trust the reference prototype's identity model
- DECISION: `TASK-260829-3jlxed` (STORY-260829-37w91q, add-pi-local-model-runtime-plane). Two processes, kept structurally apart: Process A (one Pi CLI turn) fits the existing Layer-1 `agentic.System` contract unchanged, no new lifecycle. Process B (the long-running local model server) needs a brand-new `pkg/localsupervisor` — the first process-executing code this module will ever carry, deliberately mirrored on the relux-agents-infra reference's own `model-harness`/Pi-launch-code separation rather than invented from scratch.
- DECISION: `Publisher`/`Family` land as additive, legal-blank string fields on the shared `vendorplugin.Model` struct (same non-breaking evolution path v0.2.0 used for `Lifecycle`/`Pricing`/etc). `Backend`/executable/argv stay private to a new local-models-only `RuntimeSpec` type — never added to the shared `Model` contract three of four vendors would never use.
- FINDING: the real "Pi" prototype in `relux-agents-infra` embodies exactly the anti-pattern this design exists to avoid. Its `Vendor` field is hardcoded to one of `openai|anthropic|qwen` (`project_config.go:381,406`) — so a locally-hosted Qwen checkpoint's vendor is literally the string `"qwen"`, identical to the cloud API's vendor id. No separate publisher or backend axis exists anywhere in that codebase; "mlx" only ever appears as free text inside a hand-typed `argv` array. The brief's constraint ("do not reuse Alibaba cloud auth/quota for a local resource") isn't precautionary — it's a real, present bug in the thing being used as a reference.
- FINDING: the reference prototype's readiness-polling (exact-match model id + concurrent exit-watch), restart-with-budget supervision (rolling window + fatal-substring kill), and stale-PID reclaim (compare UID+PGID+start-time+exec-shape, never trust a bare PID) are all proven, working, safe to reuse. Quarantine and log rotation are 100% absent from it — greenfield, not "already solved elsewhere."
- ROOT CAUSE (caught by independent review, fixed before handoff): first draft claimed "`local-models` vendor only reads supervisor state... true by construction, not by convention" — false. `Vendor.Availability()`/`Spawn()` are plain Go methods; nothing stops an implementation from calling `Ensure`/`Stop` from inside `Spawn()`. Fixed by splitting a narrow `StatusReader` interface (construction-time, read-only) from a separate `Supervisor` handle that only CLI/task-board wiring may ever hold, plus a dedicated import-graph regression test (test plan case 14). Lesson: "X is true by construction" is a claim about a type, not a sentence — say so only when a type actually enforces it.
- STATUS: design handed off to review (to-review). Four open questions flagged for explicit sign-off rather than force-decided: per-family vs. shared `RuntimeID` for local models; `Availability` state mapping for busy/quarantined (no 5th state exists); quarantine/log-rotation numeric defaults are unvalidated starting points; whether `pkg/localsupervisor` belongs in this module at all vs. entirely in the consumer (the single biggest architectural call in the doc).
- SCOPE: `.task-board` outcome resources on `TASK-260829-3jlxed` (`_architecture-decision.md`, `_parity-inventory.md`, `_adversarial-test-plan.md`). No production code touched. No live local-model process interacted with.

## 2026-08-24

### 0048 — A tie-free rank POSITION was a design defect, not a scheduling residue
- ROOT CAUSE: `CapabilityRank.Position` numbered a vendor's lineup 1..n with ties REFUSED, so wherever the board's scores tied — `claude-haiku-4-5` and its dated snapshot, `qwen3.7-plus` and its cross-runtime mirror, five gemini-cli/antigravity pairs under one broker — the port asserted an ordering nobody observed. `docs/shipped-state.md` had recorded this as a small residue of the consumer swap ("the consumer keeps `PolicyRank` as a SCORE"). It was the consumer telling us our rank type was wrong: its ranking consumers READ the ties.
- FIX: `CapabilityRank.Score int`, ties legal, `Basis` still required. The total order some callers need is DERIVED by `vendorplugin.Lineup` (score desc, ties broken by declaration order, `Tied` reported). `pkg/vendorplugin/lineup.go:1`. Position is no longer a field precisely because a number sitting next to an evidence-bearing score reads as a second evidence-bearing fact.
- DECISION: `Registry.Register` dropped `ErrDuplicateRank`. A shared score is the vendor STATING two models are equal; refusing it forced the declaration to invent an ordering. It gained gates that are real instead: two display picks for one agentic system, a dangling successor, a non-legacy row already replaced, a negative context window, an unquotable billing contract.
- SCOPE: `pkg/vendorplugin/{vendor,registry,runtime,lineup,plugin}.go`, all four `vendors/*/models.go`, `docs/shipped-state.md`.

### 0044 — The board's own binary is better evidence than its sources, and the sources stopped parsing
- FINDING: `.scripts/extract-model-registry.py` text-parses the board's `models.go` and can no longer read it. Since the board's `TASK-260823-1tis7o` its rows carry no `Broker`, `AgenticSystems` or `SupportedEfforts` — those come back from THIS module — so the parser `KeyError`s on `fields["Broker"]`. The existing `source-model-registry.json` is a frozen capture at `ed48781` and must NOT be regenerated.
- DECISION: New capture reads the board BINARY's own `q 'models()'` projection instead of its sources — `.scripts/capture-board-model-facts.sh`, fixture at `dbd905b`. No parser, and it cannot disagree with the binary an operator runs.
- FINDING: That projection is CIRCULAR for four columns — it joins the board's rows to this module's plugins, so `agenticSystems`/`reasoning`/`supportedEfforts`/`recommendedEffort` come BACK from here for every row with an established broker. Pinning them would be the port agreeing with itself. Fixture segregates them under `joined_from_vendor_module`; the pin skips them. The two `muse` rows are the exception the board itself names — no vendor owns them, so their effort axis is the board's own declaration and IS pinned.
- NOTE: Two captures by different means at different commits agree on all 43 ids, scores, lifecycles, 5 supersessions and 6 recommendations (`TestTheBoardFixtureAndTheOlderSourceCaptureAgree`). One capture cannot notice itself going stale.
- SCOPE: `.scripts/capture-board-model-facts.sh`, `pkg/vendorplugin/testdata/board-model-facts.json`, `pkg/vendorplugin/boardfacts_test.go`.

### 0040 — `if false` mutants die on the compiler and prove nothing
- FINDING: First mutation pass over the new gates killed 20/21, but three "kills" were Go build failures (`declared and not used`) rather than test failures. A build-failure kill proves the line exists and says nothing about the class it covers.
- FIX: Every mutant rewritten as a compile-clean NARROWING — `|| true`, `&& false` on an already-used condition, `score-1`, `len(...) > 99`, a regexp relaxed to `.`. 21/21 now killed by named tests. `.temp/TASK-260824-y7gyco/mutants.py`.
- ROOT CAUSE: One genuine survivor. Narrowing the "billing contract with no plans" refusal left the suite green — with `Plans` nil the plan loop never runs, `pricesThisModel` stays false, and the *names-this-model* refusal fires instead. Same sentinel error, different fact, test could not tell.
- FIX: `TestRegisterRefusesAnUnusablePricingContract` now states the phrase each of its 19 cases must see in its OWN refusal, and asserts case count == reason count so a case cannot be added without one. `pkg/vendorplugin/boardfields_test.go`.
- NOTE: General lesson for this repo — when every refusal in a family wraps one sentinel, `errors.Is` alone lets a broken check hide behind whichever downstream one fires next.

### 0036 — muse's rows extend the vendor-unresolved shape rather than being special-cased
- DECISION: `RuntimeDeclaration.Models`, legal ONLY when `Vendor == VendorUnresolved`; a RESOLVED runtime declaring rows is refused as "two declarations of one fact". The unresolved declaration was already COMPLETE and UNLAUNCHABLE; carrying rows makes it complete about its models without making it launchable — `ResolveRuntime` still returns `ErrRuntimeVendorUnresolved`.
- FINDING: The rows go through the same `Model.Validate` a vendor's do, plus two rules only a declaration can state (the row must name the runtime's own harness; ids unique). A second, weaker path here would be weakest exactly where nothing else is looking — these are the rows with no plugin behind them.
- FINDING: `pkg/agentic/singlesource_guard_test.go` correctly failed the build the moment `[]agentic.SystemID{"muse"}` appeared in `runtime.go`. Granted `"muse models"` → `pkg/vendorplugin/runtime.go` in `bindingHomes`, and added that file to `TestSingleSourceGuardRulesFireOnRealCode`'s required list so the new home is a permission the guard can SEE being used.
- REGRESSION: `internal/regress/declaration_test.go` F2 conflict cases broke — rebinding muse's system/vendor carried its `Models` along, so `Validate` fired before the conflict check and the test got "malformed" where it needed "already declared for another pair". Rebound declarations now drop `Models`, with the reason written down.
- SCOPE: `pkg/vendorplugin/runtime.go:270`, `pkg/agentic/singlesource_guard_test.go`, `internal/regress/declaration_test.go`.

### 0032 — Consuming docs told readers to build plumbing the consumer's own CI guard now refuses
- FINDING: `docs/consuming-the-module.md` and `SKILL.md` still described this as a **private** module needing `GOPRIVATE` and a `url.insteadOf` PAT rewrite. Stale since 2026-08-23 (repo went public, credential half retired, commit `92e6d8b`) — and worse than stale: the first consumer's `tools/board-cli/internal/ciguard` now enforces the ABSENCE of that plumbing, so a reader following the doc fails their build.
- FIX: Corrected in both, plus a dangling `tag/GOPRIVATE/go.work` phrase in `README.md`. Found while updating the same doc for the `v0.2.0` API change; flagged as out-of-scope rather than folded in silently.
- NOTE: `docs/shipped-state.md:187` already carried the retirement struck through. The ledger was right and the how-to was wrong — worth checking both directions next time a fact retires.

## 2026-08-23

### 0708 — The epic's docs closed on a board divergence: the switch story lives on the SOURCE board
- FINDING: `STORY-260821-1c5o90` (switch-task-board-to-the-tool) on THIS board is `backlog` with zero tasks. The work is real and running elsewhere: `skill-project-management`'s `STORY-260823-1sxcmg`, in `development`, with `TASK-260823-1tis7o`/`io02de`/`1rj1gf` done and `TASK-260823-t2xuen` (CI) still open. Four of this epic's six stories are on `main`, not five.
- DECISION: Documented as a board-hygiene divergence with THIS repository as owner, in `docs/shipped-state.md`, rather than silently closing either side. Two boards tracking one story is worse than one board tracking none.
- FINDING: The CI arrangement is half done and the missing half is human. `v0.1.0` is tagged at `b722ace` and pushed (`git ls-remote --tags`). The consumer side — `require` with no `replace`, `GOPRIVATE=github.com/relux-works/*`, the `url.insteadOf` rewrite, a gitignored root `go.work` — is implemented and UNCOMMITTED in the switch story's worktree. The Actions credential (repo secret `RELUX_MODULES_TOKEN`, fine-grained PAT with Contents:read) is NOT provisioned and no agent can provision it.
- NOTE: Two traps carried into `docs/consuming-the-module.md` rather than left to be rediscovered. (1) A committed root `go.work` APPLIES in CI — discovery walks UP from the working directory, so committing it reintroduces the sibling-checkout assumption removing the `replace` just took out. (2) The `secrets` context is not available to a workflow `if:` key; referencing it there is a syntax error that killed the consumer's whole CI run 2026-08-19 → 2026-08-23.
- SCOPE: `SKILL.md`, `docs/shipped-state.md`, `docs/consuming-the-module.md`, `docs/architecture.md`, `README.md`.
- STATUS: TASK-260823-4f5t1m handed to review, uncommitted.

### 0707 — `make regress`: a landing gate is only worth its negatives, and it has to be cheap
- DECISION: The regression net is `internal/regress`, a SEPARATE `make regress` target rather than a subset of `make test`, and it is appended to `spawn.worktree_isolation.validation.commands`. 14 tests, 0.45s. A landing gate that costs minutes gets skipped, and the per-package suites already own depth.
- FINDING: The gate list resolves from the MAIN checkout, not from the candidate. A candidate that ADDS a command therefore lands under the old list and the new command starts gating the next one — the same bootstrapping shape as the original gate install and as the rev-2 republish in the source's `TASK-260823-1rj1gf`. Stated in `README.md` rather than left for a reviewer to notice.
- FINDING: Class 1 needed BOTH halves to mean anything. The four real vendor plugins refused by an empty `agentic.Registry` is satisfied by a registry that refuses everything; the same vendors ADMITTED once their declared systems are moved into an isolated registry is what closes it. The declared systems are read off `Vendor.Models()`, so a vendor that grows a harness is covered with no edit.
- FINDING: Class 3 could not use `testdata/real-state/` for the suppressed verdict. The operator's live file suppresses `codex-unmapped:`, which is not a table group, so an identity-wide query never sees it — `identityVerdict` ranges over `tableGroupsFor`, deliberately, so a dead model's singleton cannot outvote every real group. `testdata/source-written/` was used instead: real bytes from the SOURCE module with a manifest carrying identity, home, group and model.
- NOTE: 10 mutants, 9 of them NARROWING production gates rather than deleting them, all caught: refuse-but-write-anyway, `SameBinding` comparing provenance, a failed read read as an absence, `probeGatedVerdict` answering healthy, a shifted suppression window, `parity.Compare` dropping the Binary field. `python3 .temp/TASK-260823-4f5t1m/mutants.py`.
- SCOPE: `internal/regress/`, `Makefile`, `task-board.config.json`.

### 0059 — The provider-home table WAS a shadow table, and the harness plugin already held the fact
- ROOT CAUSE: The source keeps `providerHomeRules` inside `pkg/providerlimits/identity.go` — a per-runtime row naming the home env var and the fallback dot-directory. Ported verbatim, `pkg/agentic/singlesource_guard_test.go` reported it. It was right: `pkg/agentic/systems/codex/codex.go:111` already declares `CODEX_HOME` / `~/.codex`, and the claude plugin's own comment (`claude.go:118`) says those fields exist "so a caller keying limit state by the resolved home has something to resolve". This is that caller.
- FIX: `DefaultProviderHome` now walks runtime → `vendorplugin.RuntimeDeclarationOf` → `agentic.Registry.Lookup` → `Capabilities().HomeEnvVar/DefaultHome`. `pkg/providerlimits/identity.go`.
- FINDING: Two source special-cases DISSOLVE rather than porting. (1) The source refuses to guess qwen's home because no evidence establishes which var qwen-code reads — here the qwen plugin declares empty for both, so the refusal falls out of the plugin's own finding. (2) The source needed a table row plus a paragraph for `qwen-codex` reusing CODEX_HOME; here an operator-declared runtime resolves through its declaration to the codex system and inherits it, with no row to write.
- FINDING: The refusal arm is only reachable if ALL SIX system plugins are compiled into the test binary. With two blank imports the four home-less harnesses were never registered and `TestTheProviderHomeComesFromTheHarnessPluginNotFromATableHere` reported it had proved nothing.
- NOTE: Cost — `pkg/providerlimits` now imports `pkg/agentic` as a second allowed intra-module import, and `AvailabilityFor` needs the harness plugin linked in. The error names the missing plugin, but a binary that forgot the import gets an error out of a plane that otherwise fails open. Flagged for the switch story.
- SCOPE: `pkg/providerlimits/identity.go`, `boundary_test.go`, `store_testhelpers_test.go`, `identity_test.go`.
- STATUS: TASK-260822-2jouz3 handed to review, uncommitted.

### 0058 — probe_eligible has no honest verdict in the four-state vocabulary, so it is a CHECKED unknown
- DECISION: `probe_eligible` and `probing` map to `AvailabilityUnknown` with a non-empty `Checked`, not to Healthy and not to Limited. `pkg/providerlimits/verdict.go` `probeGatedVerdict`.
- ROOT CAUSE: Healthy is the collapse the source warns about — probe_eligible "is admitted to a claim holder only, and stays subtracted for every other caller", and treating it as available "is what lets every concurrent preflight admit an exhausted group at once". Limited is worse than it looks: a probe-eligible group's `next_probe_at` is in the PAST, and a caller is entitled to schedule a retry off `Until`, so it would retry immediately and skip the atomic claim entirely. A probing group's lease expiry is not a clear time at all — it is when somebody else's turn ends.
- FINDING: The thing that would resolve it is the claim, and the claim is a WRITE. The verdict path is built on `LoadIdentityState` precisely so a read cannot hand out or withdraw a probe as a side effect of being looked at. So "checked, established not-available, established no time at which that changes" is the complete honest answer.
- NOTE: `Serviceable()` is false, which is the safe direction; the observation names the claim so a caller knows what to do next.
- SCOPE: `pkg/providerlimits/verdict.go`, `verdict_test.go`.
- STATUS: TASK-260822-2jouz3 handed to review, uncommitted.

### 0057 — Cross-binary round trip is the only thing that can settle a fail-open identity
- FINDING: A round trip through one implementation agrees with itself for any formula. The source's own `TestIdentityKeyIsTheDocumentedHash` re-derives the hash with the same expression the production code just used, so it cannot catch a change to that expression.
- FIX: `.scripts/capture-limit-state.sh` builds `.scripts/limitstate_xrt.go` against the SOURCE module in a scratch dir under `.temp/` (source tree read-only) and drives its real detection path to write a suppression; `.scripts/writestate` does the same with the ported code; the source's `Store.Report` then reads OUR bytes. Both directions produce byte-identical files (`sha256 3cbefb47…` claude, `660adaf6…` codex, source commit `ed48781`).
- FINDING: The operator's LIVE pre-extraction state (`~/Library/Application Support/task-board/provider-limits/`) is the strongest fixture available and reproduces both filenames from `IdentityKey` — pinned as literals in `crossbinary_test.go`. Byte-for-byte re-encode catches the class value comparison misses: renamed tag, reordered field (Go marshals in declaration order), added/dropped `omitempty`, pointer→value on a `*time.Time`.
- FINDING: The alias policy for broker-keyed backoff is probed BEHAVIOURALLY — the source's key resolution is unexported, so each candidate row is fed through its real `SpawnLimitsConfig` JSON decode. Result: `claude`/`codex`/`qwen` accepted as legacy runtime spellings, `gemini`/`agy`/`muse` rejected, brokers accepted. A re-typed list would have been one person's reading of a comment.
- NOTE: `identities.json` is deliberately NOT captured on the generated path — its `first_seen` comes off a filesystem mtime and would put a wall clock in a fixture.
- SCOPE: `.scripts/capture-limit-state.sh`, `.scripts/limitstate_xrt.go`, `.scripts/writestate/`, `pkg/providerlimits/{crossbinary_test.go,ladder_test.go,testdata/}`.
- STATUS: TASK-260822-2jouz3 handed to review, uncommitted.

## 2026-08-22

### 2356 — Admission had to keep reading the FROZEN v2 snapshot, not the ranks this port derives
- DECISION: Ported `v2AdmissionSnapshotRolloutTiers` verbatim into `pkg/vendorplugin/v2snapshot.go` and made it the only membership authority for a v2 ordered ceiling. Deriving membership from `CapabilityRank` would have reproduced the pinned digests for the current configs and reintroduced the exact defect the source spent a task removing: a truthful re-rank silently moving who may spawn.
- FINDING: The source's `PolicyRank` HAS TIES and the vendor contract refuses them. Three tie sites: the anthropic haiku alias pair (both 10), the alibaba `qwen3.7-plus` / `qwen3.7-plus-via-codex` pair (both 30), and FIVE google collisions (50/45/40/35/30) where a gemini-cli row and an antigravity row score equally because the source ranked the two harnesses independently within one broker. Positions are score-descending with declaration order breaking ties.
- FINDING: The tie break is only safe because ranks are not the admission authority. A ceiling bound at `claude-haiku-4-5-20251001` admits BOTH haiku rows through the frozen tier and would admit ONE through the derived positions. `TestAliasTierAdmitsBothDirections` is where that would surface; `TestCapabilityRanksAreNotTheAdmissionAuthority` reverses a whole vendor's lineup and requires the digest to hold.
- SCOPE: `pkg/vendorplugin/v2snapshot.go`, `pkg/vendorplugin/admission.go`, `pkg/vendorplugin/vendors/*/models.go`.
- STATUS: TASK-260822-3cknas handed to review, uncommitted.

### 2354 — A runtime is (system x vendor), and that pair is the ONLY honest way to scope a model index
- FINDING: Two vendors own rows reachable through two harnesses each — alibaba (5 `qwen-code` + 1 `codex`) and google (7 `gemini-cli` + 8 `antigravity`). Indexing a vendor's whole list per runtime gives the qwen runtime a row only the codex harness can drive: an admitted pair no launch could ever make, in a set that still looks well-formed. `RuntimeModels` filters on `DrivenBy(runtime.SystemID)` and `TestRuntimeModelsScopeToTheHarnessThatDrivesThem` pins the six counts.
- FINDING: The (system, vendor) pair is UNIQUE across all seven runtimes the source registers, so the full-set pin derives each row's runtime from that pair through the declarations instead of carrying an eighth copy of the bindings.
- NOTE: `qwen-codex` is deliberately NOT seeded into the frozen table — the source keeps it non-builtin as its own worked example of an operator-declared runtime, declared in that repo's `task-board.config.json`. It reaches this module through the public `DeclareRuntime`, and a real launch is built through it.
- SCOPE: `pkg/vendorplugin/v2snapshot.go:RuntimeModels`, `pkg/vendorplugin/sourceport_test.go`.
- STATUS: TASK-260822-3cknas handed to review, uncommitted.

### 2353 — Digests must be captured from the SOURCE BINARY, and TASK_BOARD_DIR silently redirects which config it reads
- ANOMALY: `task-board q 'project_config()'` run inside the source checkout reported `config_path` pointing at THIS repository's `task-board.config.json`, because `TASK_BOARD_DIR` was exported in the spawn environment. The digests happened to match (both configs carry the same codex/claude ceilings), so the mistake would have gone unnoticed and pinned a digest over the wrong file.
- FIX: `.scripts/capture-model-registry.sh` runs the source binary under `env -u TASK_BOARD_DIR` and then VERIFIES the projected `config_path` is the source repository's own file, refusing otherwise. It also records the config's sha256 in the fixture.
- DECISION: The capture builds the source CLI into a scratch path outside that checkout and reads its Go sources with a Python extractor, so the source stays read-only. A digest recomputed beside the port and pinned by the port proves only that the port agrees with itself.
- SCOPE: `.scripts/capture-model-registry.sh`, `.scripts/extract-model-registry.py`, `pkg/vendorplugin/testdata/source-admitted-pairs.json`.
- STATUS: TASK-260822-3cknas handed to review, uncommitted.

### 2352 — The agy preflight has no home in the plan surface, so the evidence travels on the plugin
- DECISION: `agy.New()` carries no preflight evidence and `agy.NewWithRuntime(Runtime{Executable})` carries it. That makes agy the ONE ported plugin holding state, and the divergence is stated on the type rather than discovered.
- ROOT CAUSE: The source reads `cfg.AgyRuntime` — evidence hanging off its Config. This layer's `LaunchRequest` has no field for it and must not grow one: an agy-shaped field on the type every system reads would put one harness's preflight into the core contract for five plugins to ignore. `ProbeAgyRuntime` itself runs `agy --version` and `agy --help`, so it cannot live behind any `System` method: `BuildPlan` calls all of them to build a DRY RUN. Same boundary as claude's goal preparation, different mechanism.
- FINDING: The source has TWO resolution functions with DIFFERENT refusal behaviour — `resolveAgyBinary` hard-fails with no evidence, `resolveAgyBinaryForDisplay` falls back to the bare `agy` placeholder — and this layer's `ResolveBinary` takes NO MODE, deliberately, because that is what guarantees a dry run cannot drift from the launch target. The two map onto one mode-less resolution plus one mode-aware refusal in `Argv`, which is the only plugin surface that knows the mode. Dropping the refusal instead would have been a validating gate getting weaker across a move: agy has no PATH fallback at all, so an exec plan carrying the placeholder execs whatever PATH holds.
- NOTE: `init()` registers the PREFLIGHTLESS value, and that is behavioural rather than incidental. init has no context, no deadline and nowhere to report a probe failure, so a registration that probed would make importing the package start processes. `TestTheDefaultRegistryHoldsThePreflightlessValue` drives an exec plan through `agentic.Default` and requires the refusal.
- NOTE: The dry run's no-side-effect promise is MEASURED, not asserted: a plan builds over an assignment file that does not exist and an environment carrying no PATH, and the same request under exec must fail — otherwise the dry run's success proves nothing about doing less work.
- SCOPE: `pkg/agentic/systems/agy/{runtime.go,binary.go,agy.go,args.go}`, `agy_test.go`, `parity_test.go`.
- STATUS: TASK-260822-xz8rj5 handed to review, uncommitted for the CR snapshot.

### 2350 — agy's effort is a MODEL-ID SUFFIX, and the plugin must never learn to read it
- FINDING: The source registers agy models as `gemini-3.6-flash-high`, `gemini-3.1-pro-low`, `gemini-3.5-flash-medium`; every row is `ReasoningNone` and `buildAgyArgs` references no effort value. So "agy supports high effort" is a VENDOR-layer fact about a model row, not a transport this system has.
- DECISION: The plugin declares `EffortTransportNone` and passes the model id through verbatim. That makes the refusal of a required-effort model decidable from the contract alone (`CanCarry` is false), and an explicit effort value is refused rather than dropped — which is the whole of "the effort flag is rejected" at this layer. Neither agy golden configures an effort, so both refusals have their own tests and a made-up suffix (`gemini-9.9-future-glacial`) pins the pass-through.
- NOTE: A plugin that parsed the suffix would have to decide what to do with a word it does not know, and every answer to that is Layer 1 holding a Layer-2 opinion. The parity mutant that strips `-high` from the id is byte-identical in flag COUNT and fails only because the golden compares argument values.
- SCOPE: `pkg/agentic/systems/agy/agy.go`, `args.go`, `agy_test.go`, `parity_test.go`.
- STATUS: TASK-260822-xz8rj5 handed to review, uncommitted for the CR snapshot.

### 2348 — An EMPTY environment filter is a decision, and needs a negative to be load-bearing
- FINDING: gemini, muse and agy strip NOTHING. All three are `withSpawnEnv(os.Environ(), cfg)` in the source — no `filterEnv`, no codex-family strip, no PATH sanitization — so their children inherit `CLAUDECODE`, the whole `CODEX_*` family, both service URLs, the credentials their pointers name, and the board credentials `BUG-260819-3qn52o` records. Gemini is the extreme case of that bug's general shape: its filter names nobody, so it leaks from every direction at once.
- DECISION: Each of the three keeps an `env.go` whose only code is `WithRunContext`, because an empty filter expressed by omitting the file is indistinguishable from a port that forgot — especially for a reader arriving from qwen's twelve-key filter one directory over.
- FINDING: The parity package's two permanent negatives do NOT transfer to these three. A prefix strip has no exact-key strip to be a near miss OF here, so the attack collapses into something else while pretending to measure a rule. The bound that actually bites is the opposite defect — a plugin that HAS a filter — so each carries `TestABorrowedRuntimeFilterFailsAgainstThe<X>Golden`, which bolts the shared codex-family strip onto the real plugin and requires the golden to fail, plus a narrowing showing the codex-family keys in `parent_env` are what catches it.
- SCOPE: `pkg/agentic/systems/{gemini,muse,agy}/env.go` and their `parity_negatives_test.go`.
- STATUS: TASK-260822-xz8rj5 handed to review, uncommitted for the CR snapshot.

### 2345 — The cross-plugin argv bound found a collision nobody predicted
- FINDING: `--add-dir` was in agy's argv signature until the guard reported the CODEX plugin's own `Args`. Codex spells it too, to hand its child the board directory (source `adapter.go:332`). The literal is still in agy's real argv; it is simply not evidence that a function is spelling AGY argv, and it is now excluded with the residual it leaves demonstrated staying open.
- NOTE: This is the cross-plugin false-positive test doing exactly what it exists for, on the first task where five siblings existed to measure against. The rule the claude port wrote down — check every candidate literal against the OTHER agents' builders before adopting it — was applied by hand for `--dangerously-skip-permissions` and `--output-format` and still missed this one. The test found what the reading did not.
- DECISION: Every new plugin's guard now scans all five sibling packages rather than one, so the next collision is reported rather than argued about.
- SCOPE: `pkg/agentic/systems/{qwen,gemini,muse,agy}/argvguard_test.go`.
- STATUS: TASK-260822-xz8rj5 handed to review, uncommitted for the CR snapshot.

### 2342 — An aliasing test that lets the append reallocate proves nothing
- ROOT CAUSE: `TestArgsDoesNotAliasTheCallersPrefix` was written as "mutate `args[0]`, check the caller's `prefix[0]`", with a prefix of len 2 cap 8. qwen's `Args` appends EIGHT arguments, so the append overflowed the capacity, Go reallocated, and the aliasing plugin looked correct. The mutation harness caught it: the "prefix not copied" mutant was the one survivor of the first run.
- FIX: The test now asserts against the caller's BACKING ARRAY — a 64-element array of sentinels with a two-element window handed over — so an aliasing plugin is caught whatever the capacity arithmetic does. Added to all four new plugins, and all four mutants are now caught.
- NOTE: The claude version of this test passes only because its `Args` appends few enough arguments to stay inside the capacity. It is correct today by arithmetic, not by construction.
- SCOPE: `pkg/agentic/systems/{qwen,gemini,muse,agy}/*_test.go`.
- STATUS: TASK-260822-xz8rj5 handed to review, uncommitted for the CR snapshot.

### 2340 — qwen's CLAUDECODE strip is a paid-for fix, and the source single-sources the rest
- FINDING: `filterQwenRuntimeEnv` is `filterCodexRuntimeEnv(filterEnv(environ, "CLAUDECODE"))`, and its own comment says the composition is deliberate: it "keeps this single-sourced instead of restating the list". The `CLAUDECODE` half is the source's `TASK-260817-2eo4ok` fix — before it a qwen child launched inside a Claude Code session inherited the nesting marker and refused to start.
- DECISION: The eleven codex keys, the exact-key filter, the credential-pointer resolution and the PATH sanitizer moved to `internal/runtimeenv`, shared by the codex and qwen plugins; the twelfth key stays spelled in qwen's `env.go` because it IS the difference between the two contracts. A per-plugin copy of the list is the drift that comment closed, and it would be invisible: a child that inherits one key too many still launches.
- FINDING: The reverse leak points at qwen itself, not only at its neighbours. `BUG-260819-3qn52o` records that `QWEN_CODE_SESSION_ID` is not stripped from claude and codex children; nothing strips it from a QWEN child either, so a qwen child spawned inside a qwen session inherits its parent's session id. Third open leak, named in `env.go` and pinned by a test asserting the current behaviour.
- NOTE: qwen is the fixture the four bystander keys were seeded FOR — its filter strips every other key in the pinned environment, so without them a port that discards the whole parent environment byte-matches. Both permanent negatives are driven through the shipped plugin, each with its narrowing.
- SCOPE: `internal/runtimeenv/`, `pkg/agentic/systems/qwen/env.go`, `env_test.go`, `parity_negatives_test.go`, `pkg/agentic/systems/codex/env.go`.
- STATUS: TASK-260822-xz8rj5 handed to review, uncommitted for the CR snapshot.

### 2136 — A refusal probe that varies one field too many proves the WRONG rule
- ROOT CAUSE: A negative case is only evidence for the rule it names if that rule is the ONLY one that can refuse it. `claude/composition.go` validates each server against several rules in sequence; four of its refusals had a named case that tripped an EARLIER rule, so the named refusal was never executed and deleting it left the whole suite green. This is the third recurrence of the shape recorded at 2026-08-22 2132 (the codex unknown-field probe that substituted a field FOR the url and was refused by the transport rule instead).
- FINDING: Mutating EVERY refusal branch and sub-condition in `composition.go`, rather than reading for the pattern, found nine unbound branches where review had identified two. Four were shadows — unknown-server, duplicate-server, unnamed-server, and `entry.Type != server.Transport` — and five were sub-conditions with no case at all (http with no url, http with args, stdio with headers, stdio with no command, `root.MCPServers == nil`). A targeted re-read of the two the reviewer named would have left seven in place.
- FINDING: The shadowing mechanism is worth naming because it will recur: an undeclared server name resolves to the ZERO `CompositionServer`, whose `Transport` is `""`. Any probe that names a transport therefore disagrees with that zero value and is refused by the per-transport rule BEFORE the existence check runs. The same zero-value effect shadows the unnamed-server rule, and the "last declaration wins the map" effect shadows the duplicate rule whenever the probe also varies the transport.
- DECISION: Each isolating probe is built by varying only the field its rule reads: `"type":""` for the existence rule, an identical transport with a differing `BearerTokenEnvVar` for the duplicate rule, a matching whitespace key on both sides for the name rule, and an entry valid under the metadata's own shape with only `type` flipped for the transport-equality rule. The rule is now stated in `TestTheCompositionValidatorRefuses`'s doc comment, with a per-case comment saying what must not be "tidied" back.
- NOTE: The duplicate case's shape matters beyond the mutant. Varying the transport tests nothing; varying the CREDENTIAL is the dangerous form — a reviewer reading the first declaration believes the child reads `REVIEWED`, the map keeps the last, and the child reads `SNEAKY`.
- NOTE: A capability field is not pinned by being declared with a comment saying it may not move. `Capabilities.HomeEnvVar`/`DefaultHome` carried exactly that comment and both moved silently through the whole suite; codex pinned the identical pair and caught it. On-disk limit state is keyed by the home these resolve to.
- SCOPE: `pkg/agentic/systems/claude/composition_test.go`, `claude_test.go`, `args_test.go`. No production behaviour changed: 21 mutants, 21 caught, 0 survivors.
- STATUS: TASK-260822-3u97y3 rework handed to review, uncommitted for the CR snapshot.

### 2325 — Goal-mode launch preparation cannot live behind any plugin method
- DECISION: The claude port carries goal mode's ARGV half and explicitly leaves its PREPARATION half out of the plan surface. `pkg/agentic/systems/claude/goal.go` states the whole of what the source does there.
- FINDING: `PrepareClaudeGoalLaunch` (source, `spawn/claude_goal.go`) does four things before construction: validates the board goal contract, gates on Claude Code >= 2.1.139 by running `claude --version`, probes `/goal` capability by running `claude -p --output-format json /goal`, and decides a session action (`native_goal_bound` / `binding_retained` / `successor_required`). Steps two and three START PROCESSES. `BuildPlan` calls EVERY `System` method to build a DRY RUN, so preparation behind `ResolveBinary`, `Argv` or `Stdin` would make a dry run execute the harness twice — the exact drift this repo's dry-run-as-a-mode design exists to prevent. Step four is not a plan fact at all: it decides about a session that already exists, which the source itself records by making `ClaudeGoalBinding.Rollback` a documented no-op.
- DECISION: One piece of preparation IS carried across — the refusal of a goal whose provider condition is empty. Without it a goal-bound launch ships the literal argument `/goal ` and the child is told it is bound to nothing, with no error anywhere. A validating gate must not get weaker across a move; this repo has recorded that failure once already, on `CompositionServer.BearerTokenEnvVar`.
- NOTE: Home for the rest is the launch/session plane when that is ported, in the position `PreflightClaudeGoalLaunch` holds in the source's command layer. This task invents no home for it.
- SCOPE: `pkg/agentic/systems/claude/goal.go`, `args.go`, `args_test.go`.
- STATUS: TASK-260822-3u97y3 handed to review, uncommitted for the CR snapshot.

### 2320 — The claude env contract is ONE key, and the codex plugin is the trap
- FINDING: A claude child in the source inherits the whole `CODEX_*` family, both app-server/session-manager URLs, the credentials their `*_AUTH_TOKEN_ENV` pointers name, and `TASK_BOARD_SESSION_ID`. `buildClaudeCommand` is `withSpawnEnv(filterEnv(os.Environ(), "CLAUDECODE"), cfg)` — one exact key. `filterCodexRuntimeEnv` is a CODEX child's filter; `filterQwenRuntimeEnv` is that PLUS `CLAUDECODE` for a qwen child. Claude's is neither.
- NOTE: The spawn brief for this task asserted the opposite ("the codex family is stripped from claude children") and named the goldens as the authority. The goldens settle it: `env_removed` for both claude cases is `CLAUDECODE` plus the four run-context keys `withSpawnEnv` replaces, and every seeded `CODEX_*` key survives. Read the code and the fixture, not the brief.
- FINDING: This is `BUG-260819-3qn52o` seen from the other side, and the general shape is what matters: every filter names the OTHER runtimes it knew about when it was written, so a seventh runtime opens a leak in both directions by existing. Not fixable one plugin at a time, and not fixable inside a parity port whose acceptance is byte-identical launch surfaces.
- NOTE: Two claims no golden can hold, so both got their own test: the codex family SURVIVING (`TestTheClaudeChildKeepsTheCodexFamily` — the goldens show it, but only as an absence from `env_removed`), and PATH not being sanitized (the capture excludes PATH from its diff entirely; the fixtures README names the residual).
- DECISION: The strip is `agentic.SetEnvValue(env, key, "")` rather than a local loop. Whole-key matching already has one implementation in this module and a second would agree until the day one was widened.
- SCOPE: `pkg/agentic/systems/claude/env.go`, `env_test.go`, `parity_negatives_test.go`.
- STATUS: TASK-260822-3u97y3 handed to review, uncommitted for the CR snapshot.

### 2312 — A bare-name argv allowlist stops working the moment a second plugin lands
- ROOT CAUSE: The codex argv guard's allowlist was keyed by FUNCTION NAME. The claude plugin names its construction site `Args` too — as every plugin will — so a bare-name allowlist exempts every `Args` in the module from every plugin's guard. The codex guard would have silently lost the ability to report a second codex construction named `Args` one directory over, and nothing would have failed.
- FIX: Allowlist keys are `<module-relative file>::<name>` (`internal/argvguard.AllowlistKey`). Both plugins' guards carry a mutant planting the identical construction under a different filename with the allowlisted name; both must report it.
- DECISION: The scanner itself moved to `internal/argvguard`, shared, and the module-source walk to `internal/gosources`, shared with `pkg/agentic`'s single-source guard. Two scanners with the same threshold today are two definitions of the rule tomorrow, and both report clean when they disagree — which is the failure the rule exists to prevent. Each plugin now supplies only what is genuinely its own: the signature literals and the allowlist.
- FINDING: A signature literal has to be checked against the OTHER agents in the source before it is adopted. `--dangerously-skip-permissions` is claude's most characteristic flag AND is spelled by `buildAgyArgs` (`spawn.go:1185`); `--output-format` is spelled by qwen and agy. Including either would make claude's guard fire on a legitimate sibling plugin the day one is ported, and a guard that reports honest code is a guard somebody deletes. Both are excluded, the resulting residual (a copy of the unconditional prompt-mode flags alone) is named on `claudeArgvSignature`, and `TestTheDeclaredResidualStaysOpen` holds the comment to the code.
- SCOPE: `internal/argvguard/`, `internal/gosources/`, `internal/launchenv/` (new), `pkg/agentic/systems/{codex,claude}/argvguard_test.go`, `pkg/agentic/singlesource_guard_test.go`, `pkg/agentic/singlesource_scanscope_test.go`.
- STATUS: TASK-260822-3u97y3 handed to review, uncommitted for the CR snapshot.

### 2305 — The goal predicate is rendered by the board, so a plugin can only carry it
- DECISION: `agentic.Goal` gained `ProviderCondition`. The claude adapter reads `goal.ProviderCondition` and splices `/goal <condition>` into argv; the TEXT is rendered by `pkgboard.RenderGoalProviderCondition` and the board validates a stored contract against a RE-render.
- FINDING: So a plugin that rendered the predicate itself would be a second renderer for a fact the board owns, producing a directive the board rejects as tampered. Without the field the only alternatives were that second renderer or dropping the binding entirely. It is deliberately NOT `Objective`: the objective is operator intent and reaches claude's argv and environment nowhere at all.
- NOTE: The claude parity test spells the renderer's FORMAT STRING and `fmt.Sprintf`s it rather than copying the rendered text out of the fixture it compares to. A case that reads its expected value out of its own golden proves only that JSON round-trips.
- SCOPE: `pkg/agentic/system.go`, `pkg/agentic/systems/claude/goal.go`, `parity_test.go`.
- STATUS: TASK-260822-3u97y3 handed to review, uncommitted for the CR snapshot.

### 2140 — Binary resolution that reads the process environment cannot be dry-run-proven
- DECISION: The codex plugin resolves its binary from `LaunchRequest.Env`, not from `os.Getenv`/`exec.LookPath`. `pkg/agentic/systems/codex/binary.go`.
- NOTE: The source read the ambient process env for both `CODEX_MANAGED_PACKAGE_ROOT` and `PATH`. Under the plugin contract that is unprovable: `System` promises a plugin reaches for no ambient state and that a dry run reports the same target a real launch would, and neither claim survives a process-global. Reading the handed environment also makes all three resolution paths testable in parallel against stub layouts with no `t.Setenv` anywhere.
- FINDING: The reshape is invisible in the goldens for a reason worth knowing. The capture's managed-npm case set the package root with `t.Setenv` AFTER recording its baseline, so `capture.parent_env` carries the pinned placeholder while the code under capture saw a temp layout. A port test that repoints the launch environment reproduces exactly that state — and the strip removes the key from the child either way, so the recorded diff is identical.
- NOTE: An absent PATH and an empty PATH are separate answers (`ErrNoPathInLaunchEnvironment` vs not-found). A caller that curated an environment without PATH must not be told its codex install is missing.
- SCOPE: `pkg/agentic/systems/codex/binary.go`, `binary_test.go`.
- STATUS: Resolved (TASK-260822-hp5fb4).

### 2138 — A prefix env strip does not only over-strip; it breaks pointer resolution and leaks the credentials
- FINDING: `filterCodexRuntimeEnv` carries no fixed list of credential variables. It reads `TASK_BOARD_CODEX_APP_SERVER_AUTH_TOKEN_ENV` and `TASK_BOARD_SESSION_MANAGER_AUTH_TOKEN_ENV`, each holding the NAME of the variable that holds a token, and blocks what they name. A port that strips the `TASK_BOARD_` family by prefix removes those pointers BEFORE the filter can read them, so the tokens they point at are never blocked and reach the child.
- NOTE: So the prefix reading has two independent consequences — it over-strips the near-miss bystanders AND under-strips exactly the credentials the filter exists to contain. Found while porting, not predicted: the first prefix-strip attack failed its own narrowing subtest because the credential leak was still catching it after the bystanders were removed.
- NOTE: Consequence for the attack's shape. The narrowing that isolates the exact-key property uses the `CODEX_` prefix ALONE, which disturbs no pointer; the credential leak gets its own test with the full family set. One attack proving two things proves neither cleanly.
- SCOPE: `pkg/agentic/systems/codex/parity_negatives_test.go`, `env_test.go`.
- STATUS: Resolved (TASK-260822-hp5fb4).

### 2135 — The codex goldens have a fifth survivor the capture script does not maintain
- FINDING: `CLAUDECODE` survives a codex child incidentally — `filterCodexRuntimeEnv` has no entry for it; stripping it is `filterQwenRuntimeEnv`'s addition. So codex/exec preserves 5 of 22 pinned keys, not the 4 seeded bystanders, and the wipe attack's narrowing fails if it removes only the four.
- NOTE: The distinction is load-bearing rather than pedantic. A seeded bystander is maintained and a test fails if a recapture drops it; an incidental survivor would vanish the day someone adds `CLAUDECODE` to the codex filter for a good reason, and a preservation bound resting on it would break silently. The wipe test therefore carries two narrowings: remove ALL survivors and the bypass returns, remove only the incidental one and the four seeded keys still catch it.
- SCOPE: `pkg/agentic/systems/codex/parity_negatives_test.go`.
- STATUS: Resolved (TASK-260822-hp5fb4).

### 2132 — Two gates were decoration until a mutation run said so
- ANOMALY: 31 mutants over the codex plugin's production gates; 29 red, 2 survived a fully green suite.
- FINDING 1: the composition validator's closed-field check. The negative test substituted an unknown field FOR the url, so the per-transport shape rule ("an http server needs a url") refused it and the field check itself was never exercised. Fixed by ADDING the unknown field to an otherwise valid composition, which only the closed-set check can refuse.
- FINDING 2: `childEnv`'s filter-then-inject ORDER. The comment claimed the order was load-bearing; swapping it left the whole suite green, because no key in the codex blocked set is also a run-context key. The comment was wrong, not the tests. Corrected to state what is true today, plus `TestNoStrippedKeyCollidesWithAnInjectedOne` pinning the non-overlap — so the day a collision arrives it is a failing test naming the comment rather than a child that lost its run id to its own filter.
- NOTE: Both survivors were tests that PASSED for the wrong reason. Neither would have been found by adding more assertions; only breaking the production code found them.
- SCOPE: `.temp/TASK-260822-hp5fb4/mutants.py`, `mutants-01.log` (29/31), `mutants-02.log` (31/31).
- STATUS: Resolved (TASK-260822-hp5fb4).

### 2128 — The plugin contract could not express what withSpawnEnv read off the config
- DECISION: `LaunchRequest` gained `Run RunContext` (run id, task id, board dir, context id) and `Profile`; `CompositionServer` gained `BearerTokenEnvVar`; `pkg/agentic/runcontext.go` carries the source's `withSpawnEnv`/`appendOrReplaceEnv` as `WithRunContext`/`SetEnvValue`.
- NOTE: Not optional. The codex goldens record `TASK_BOARD_RUN_ID`, `TASK_BOARD_TASK_ID`, `TASK_BOARD_BOARD_DIR` and `TASK_BOARD_DIR` in `env_added`, and `-p <profile>` / `--add-dir <board>` in the argv. A plugin that reaches for nothing ambient cannot produce any of them unless they arrive on the request.
- DECISION: the run-context injection lives in the CORE, not in each plugin. The source had exactly one `withSpawnEnv` shared by all six adapters; six copies here would be six sources for one fact, and the drift would be invisible because a system that injects one variable differently still launches.
- NOTE: `CompositionServer.BearerTokenEnvVar` is the smaller but sharper one — without it the codex validator can only DROP its two bearer checks, which is a validating gate quietly getting weaker across a move.
- SCOPE: `pkg/agentic/system.go`, `pkg/agentic/runcontext.go`, `runcontext_test.go`.
- STATUS: Resolved (TASK-260822-hp5fb4).

### 2125 — Three source behaviours pinned as-is rather than improved mid-port
- DECISION: The codex plugin keeps, and tests assert, three behaviours that read as defects: `BUG-260819-3qn52o`'s two open child-env leaks (`TASK_BOARD_TOKEN`/`TASK_BOARD_BUILDER_GATEWAY_TOKEN` reaching every provider child, and another runtime's session state not stripped); a service-tier export guarded by `if tier != ""`, so a launch configuring no tier lets the parent's value pass through to the child; and an unrecognized tier normalizing to nothing and being dropped with no refusal.
- NOTE: The bug is in backlog at the commit the goldens were captured from. Its own AC3 says why it cannot be closed one plugin at a time — the construction has to make a seventh runtime safe without editing N filters — so fixing it in one of six ports would leave two implementations of one contract disagreeing.
- NOTE: The tier drop sits awkwardly beside this repo's own rule that a parameter a system cannot honour is REFUSED rather than dropped (`ErrServiceTierUnsupported`). Pinned where a reader will find it rather than changed in a parity port.
- NOTE: Pinning current behaviour is the mechanism, not the point: if someone closes a leak, the test goes red and names the bug, which is the conversation that should happen. A comment alone would have let the state change silently.
- SCOPE: `pkg/agentic/systems/codex/env.go`, `env_test.go`.
- STATUS: Open by design (BUG-260819-3qn52o remains the source's to close).

### 1915 — A seeded env diff proves a filter's lower bound only; bystander keys buy the upper bound
- FINDING: Seeding the pinned capture environment with exactly the keys a filter strips proves the filter removes AT LEAST those keys and says nothing about whether it removes more. `qwen/exec` was the fixture where that gap became a bypass: `filterQwenRuntimeEnv` is `filterCodexRuntimeEnv` plus `CLAUDECODE`, which between them stripped all 18 seeded keys, so `env_removed` covered 100% of `parent_env` and a port whose `ChildEnv` discards the whole parent environment and returns only its injections byte-matched the golden through the real `agentic.BuildPlan` — zero differences.
- ANOMALY: The golden's SHAPE invited the defect. A port author reading "everything removed" writes "return only the injections" and passes. Review found it by building that port; the goldens had no way to report it.
- FIX: Four bystander keys in `PINNED_ENV` (`.scripts/capture-parity-goldens.sh`) that no source filter touches, and a full regeneration at the same source commit. `PARITY_BYSTANDER` is the plain preservation bound; `CODEX_LIKE_BUT_NOT`, `CLAUDECODE_LIKE_BUT_NOT` and `TASK_BOARD_LIKE_BUT_NOT` are near-misses that pin the strip to an EXACT key set — the source blocks by key map (`filterEnvKeys`, `spawn.go:998`) and replaces by whole key (`appendOrReplaceEnv`), never by prefix. `qwen/exec` now preserves 4 of 22; every other system gained the same bound.
- NOTE: Each attack ships with its own narrowing, because "the suite catches it" is not the same claim as "these keys catch it". `TestAWholeEnvironmentWipeFailsAgainstQwenExec` and `TestAPrefixStripFailsAgainstQwenExec` each strip the relevant bystanders back out of the recorded parent env in a subtest and assert the same defect walks through. Removing all four from `PINNED_ENV` and regenerating turns three tests red, including the one that names the script line to fix.
- NOTE: Provenance re-verified rather than assumed after the reseed — two full captures under different `t.TempDir` allocations produced byte-identical fixtures, so masking is doing real work. Source checkout `ed4878123061b39fdae67160f6b5632117b48a2f`, clean before and after, one `go test` inside it.
- SCOPE: `.scripts/capture-parity-goldens.sh`, all 14 goldens, `pkg/agentic/parity/{plan_test.go,golden_test.go}`, `pkg/agentic/parity/testdata/goldens/README.md`, `README.md`.
- STATUS: TASK-260822-slgewd review rev1 blocking finding resolved; rev2 handed to review, uncommitted for the CR snapshot.

### 1853 — A pinned parent environment is what makes a captured env diff portable
- DECISION: The launch-surface goldens (`pkg/agentic/parity/testdata/goldens/`) were captured by running the EXTRACTION SOURCE's own `TestCaptureLaunchSurface` under `env -i` with a synthetic parent environment, not under an ordinary shell. The source harness diffs the child env against `os.Environ()`; its own `parity-after.json` therefore carries `TASK_BOARD_BOARD_DIR=/Users/alexis/src/relux-works/skill-project-management/.task-board` and this session's run ids. `.scripts/capture-parity-goldens.sh`.
- FINDING: The real defect an unpinned capture hides is not the leaked paths, it is the SILENCE. `env_removed` can only report a key the parent actually had. A plugin's strip of `CODEX_SESSION` or `TASK_BOARD_SESSION_ID` is provable only on a machine that happened to set it — everywhere else the golden records nothing and the port passes for want of evidence. The pinned set seeds exactly the keys `filterCodexRuntimeEnv`, `filterQwenRuntimeEnv` and `withSpawnEnv` act on, so each strip and each replacement is observable. `TASK_BOARD_DIR` is deliberately absent so the child's injection of it shows up as an addition.
- DECISION: Each golden records its own `capture.parent_env`, and `ComparePlan` feeds THAT to the plan rather than letting the caller supply one. Env added/removed is a diff; a port supplying its own baseline compares a different measurement wearing the same schema. `TestAPlanBuiltWithoutTheGoldensParentEnvIsNotSilentlyAccepted`.
- DECISION: PATH pinned to a stub-executable directory, so the dry-run cases resolve deterministically. Deviates from the source's artifact, which recorded its operator's installed codex and `executable file not found` for qwen/muse. An `error` fixture pins a refusal instead of the dry-run argv grammar — which is the thing the ports must reproduce — so the deviation buys the contract back. Written up in the fixtures README rather than left for a reader to hit.
- FINDING: Masking must be ONE implementation used by both sides. Generation discovers the machine-local literals (`DiscoverSubstitutions`, regex over the capture layout) and then calls the SAME `Mask` the comparison calls. A regex masker at generation and a literal masker at comparison would agree until the day they did not, and the disagreement would read as a port bug.
- NOTE: Three pins on masking, because masking is where a real difference goes invisible while the suite stays green: rule set frozen as a literal; the set of snapshot FIELDS masking may rewrite frozen and checked by planting a maskable literal in every field at once; each rule narrowed one at a time until a real golden stops matching. `pkg/agentic/parity/mask_test.go`.
- SCOPE: `pkg/agentic/parity/` (new package, 14 goldens, 45 tests), `.scripts/capture-parity-goldens.sh`, `README.md`. Source checkout read-only throughout — one `go test`, clean before and after.
- STATUS: TASK-260822-slgewd handed to review, uncommitted for the CR snapshot. Source commit pinned in every fixture: `ed4878123061b39fdae67160f6b5632117b48a2f`.

## 2026-08-21

### 2015 — A guard's scan scope is part of its rule, not plumbing
- ROOT CAUSE: `moduleSources` excluded directories by NAME — `.git`, `.temp`, `.task-board`, `.claude`, `.codex`, `vendor`, `node_modules`, `testdata` — which is a list of the machine-local trees somebody had thought of by then. A bootstrapped checkout has agents-infra installed at `.agents/`: gitignored, present only where agents-infra installed it, carrying its own `go.mod`, and dispatching on `"codex"`/`"claude"`. The guard reported 12 violations there and `go test ./...` went red on main while every worktree stayed green. The suite's colour depended on whose machine ran it.
- NOTE: Timestamp is real local MSK; earlier entries today were stamped from a clock ~1.5h ahead, so this sits above them by append order, not by stamp.
- FIX: `skipModuleDir` mirrors `go/build`'s own rules instead of naming directories — leading `.` or `_`, plus any subtree carrying its own `go.mod`. `vendor`/`node_modules`/`testdata` stay named. The root is never skipped on its own name: this checkout habitually lives under `.temp/`, and testing the root would scan zero files while reporting clean. `pkg/agentic/singlesource_guard_test.go`, and the duplicate walk at `pkg/vendorplugin/double_test.go:349`.
- DECISION: `testdata` is EXCLUDED, matching `go build`. The scan set and the build set become the same set with no third rule; the cost is that a binding parked in `testdata` goes unreported. A guard fixture wanting a violating source writes it into a temp tree whose root it also controls instead. Argued in the `# Scan scope` threat-model section, pinned by a `testdata` case nested inside a scanned package.
- FINDING: The exclusion is directional and BOTH directions are failures — too wide reddens trunk on machine-local code, too narrow silently unguards a package while CI stays green. So every case is planted twice: the SAME violating source inside a dot-dir, an underscore-dir, a nested module, `testdata` and `vendor` (must not be reported) and in two ordinary packages (must be). `pkg/agentic/singlesource_scanscope_test.go`.
- FINDING: The load-bearing test is the one that runs FIRST — `TestSingleSourceGuardScanScopeFixtureIsViolating` scans every planted path directly, bypassing the walk, and requires all ten reported. Without it, every "not reported" assertion is equally consistent with a fixture that violates nothing, and the exclusion could widen to swallow the whole module while the file stayed green. A skip assertion is worth exactly what the proof that the skipped thing WAS a violation is worth.
- FINDING: A `stat` failing for a reason other than not-exist is propagated, not read as absence. Collapsing the two turns an unreadable subtree into a silently unscanned one that looks like a clean scan. `TestSkipModuleDirUnreadableIsNotAbsent` uses a 0000 directory and skips itself if the deny did not take effect, so it cannot pass vacuously.
- NOTE: Bound proven by NARROWING and by WIDENING, not only by deleting. 5 mutants on `skipModuleDir`: dot rule deleted, nested-go.mod rule narrowed to never fire, exclusion widened to every subdirectory, root exemption removed, stat failure read as absence. All killed, each by the test claiming that class.
- SCOPE: `pkg/agentic/singlesource_guard_test.go`, `pkg/agentic/singlesource_scanscope_test.go` (new), `pkg/vendorplugin/double_test.go`, `pkg/vendorplugin/scanscope_test.go` (new), `README.md`. No production source changed. 58-mutant matrix green (66 named subtests = 58 caught + 8 declared-open residuals).
- STATUS: Resolved (BUG-260821-3o0xr3), handed to review. Verified against the real bootstrapped `.agents` tree: old walk exit 1 with `child_launch_composition.go:95` verbatim, new walk exit 0.

### 2140 — A bound the caller happens to satisfy is not a pinned bound
- FINDING: `EffortDeclaration.Accepts` trims the word before matching, and so does `BuildLaunch` at `spawn.go:162` BEFORE calling it. With the case-sensitivity test driving only `BuildLaunch`, deleting `Accepts`' own `TrimSpace` left the entire suite green (`go test ./... -count=1` → exit 0). The bound looked pinned and was being satisfied by a caller upstream of it.
- NOTE: The general shape: when two layers apply the same normalization, a test that enters at the outer one cannot observe the inner one at all. Every mutant of the inner check is invisible, so the inner check reads as tested and is not. It is the delete-only-mutant failure one level down — the test proves the PATH works, not that either check does.
- FIX: `TestEffortVocabularyMembershipIsExactOnTheTrimmedWord` drives `Accepts` directly, alongside `TestBuildLaunchDoesNotFoldTheEffortVocabulary` at the entry point. Both are load-bearing against different mutants: M11 (`Accepts` stops trimming) reds only the first, M12 (`BuildLaunch` stops trimming) reds only the second.
- NOTE: `Accepts` is exported and has a second production caller — `EffortDeclaration.Validate` asks it about the vendor's own `Recommended` — so it is contract surface, not a private helper the launch path happens to use.
- SCOPE: `pkg/vendorplugin/spawn_test.go`. Mutation evidence: `TASK-260821-atcotl_rev2-mutant-results.json`.
- STATUS: Resolved (TASK-260821-atcotl rev 2).

### 2135 — Naming a test for distinctness does not test distinctness
- FINDING: `TestResolutionRefusalsAreDistinctFacts` asserted four positive `errors.Is` calls and nothing else. Aliasing `ErrRuntimeVendorUnresolved = ErrRuntimeVendorUnregistered` left the whole suite green — the muse decision (entry 2015) rests entirely on callers branching on that typed difference, and after the collapse muse tells an operator to install a vendor nobody has identified.
- NOTE: `errors.Is(err, want)` proves a branch is REACHABLE. Mutual exclusivity is a separate claim and needs the negative half: `!errors.Is(err, other)` for every other sentinel a caller might branch on. Four positives cannot distinguish four sentinels from one.
- FIX: A table of four resolution refusals × five sentinels, each case asserting its own and refuting the other four. Table-shaped rather than one hand-written negative pair, so collapsing ANY pair goes red — not only the pair that was found.
- NOTE: The unresolved case resolves the real `muse` row through `SeedFrozenRuntimes` rather than a hand-built lookalike declaration, which needed a system registered under the id `muse` — `renamedPangolin` in `double_test.go`, the existing Layer-1 double under a different id rather than a second fake.
- SCOPE: `pkg/vendorplugin/runtime_test.go`, `pkg/vendorplugin/double_test.go`. Related: entry 2015.
- STATUS: Resolved (TASK-260821-atcotl rev 2).

### 2015 — An unresolved vendor is the absence of a vendor, not a degraded one
- DECISION: `muse`'s broker is carried as `VendorUnresolved` (the empty `VendorID`) plus a REQUIRED `BrokerProvenance{Checked, Found}`. Resolution refuses it with its own error, `ErrRuntimeVendorUnresolved`, distinct from "the vendor plugin is not compiled in".
- NOTE: The alternative — a null-object vendor with an empty model list — would have put a fake plugin behind a real interface, and every caller would have had to know which vendors are real. Making the runtime UNLAUNCHABLE through the vendor layer costs nothing today (no spawn execution exists yet) and keeps the interface honest for everyone else.
- NOTE: The provenance is what makes it a finding rather than an unfilled field. A blank vendor with no `Checked` and a blank vendor after a real search are indistinguishable without it, and they are different facts. Validation enforces both directions: a resolved vendor must record what established it, an unresolved one must record where it was looked for AND must not claim a finding.
- SCOPE: `pkg/vendorplugin/runtime.go`, `TestMusesBrokerIsCarriedAsCheckedAndEmpty`, `TestResolvingAnUnresolvedVendorRuntimeIsRefusedOnItsOwnTerms`.
- STATUS: Resolved (TASK-260821-atcotl).

### 2014 — A per-key-type home list is strictly stronger than a file allowlist
- FINDING: Extending the single-source guard to the vendor layer had two shapes. A flat allowlist ("these files may hold binding tables") would have let `pkg/agentic/registry.go` bind vendors and `pkg/vendorplugin/registry.go` bind systems — a shadow table in the one place nobody would look, because the file legitimately holds a binding map.
- DECISION: `bindingHomes` maps each dispatch KEY TYPE (`SystemID`, `VendorID`, `RuntimeID`) to the one file permitted to bind it. The invariant is one home per FACT, and "which plugin implements system X" and "which plugin implements vendor Y" are two facts.
- NOTE: The narrowing mutant is the proof and nothing else is: flattening `misplaced()` into "is this file any home" reds ONLY `TestSingleSourceGuardCatchesCrossLayerBindings` and leaves all 58 other guard mutants green. Without that test the two designs are indistinguishable from a green suite.
- NOTE: `TestSingleSourceGuardRulesFireOnRealCode` had to change with it. Passing an EMPTY home list now resolves no key types at all, so an empty violation list would mean "the scanner had nothing to look for" — vacuously green. It displaces every home to a nonexistent path instead.
- SCOPE: `pkg/agentic/singlesource_guard_test.go`, `pkg/agentic/singlesource_guard_mutants_test.go`.
- STATUS: Resolved (TASK-260821-atcotl).

### 2013 — A vendor may add to a launch; it may not redirect one
- DECISION: `BuildLaunch` checks the `agentic.LaunchRequest` a vendor returns against what was admitted — system, model id, effort support, effort word, goal, budget, service tier, composition — and refuses a mismatch as a plugin-contract violation. Environment additions pass through untouched.
- NOTE: Without the check, a vendor plugin could silently move a run to another harness, another model or an unbounded budget, and the result would look exactly like the run that was asked for. That is the same failure class as the source repo's dropped-effort launch, one layer up.
- NOTE: The control matters as much as the gate: `TestBuildLaunchKeepsTheVendorsOwnAdditions` fails an implementation that ignores the vendor's answer and rebuilds the request itself, which would pass all nine redirection mutants.
- SCOPE: `pkg/vendorplugin/spawn.go` (`checkLaunchFidelity`).
- STATUS: Resolved (TASK-260821-atcotl).

### 2012 — The vendor's recommended effort is a declaration, not a default
- FINDING: A model row carries both an effort vocabulary and a recommended word. The obvious convenience — fill in the recommendation when the caller supplies nothing — is exactly the wrong-cost launch invariant 4 exists to close.
- DECISION: A missing effort is a refusal whose text names the vocabulary AND the recommendation, so an agent hitting it mid-run can fix the call from the error alone. The recommendation reaches the operator through the refusal, never through the argv.
- NOTE: The gate is proved by where the vendor is NOT called: `TestAVendorNeverSeesAnUnadmittedEffort` fails if `Spawn` is reached with an unvalidated effort, which closes the path on which a plugin could supply its own default downstream of the check.
- SCOPE: `pkg/vendorplugin/spawn.go` (`resolveEffort`).
- STATUS: Resolved (TASK-260821-atcotl).

### 2011 — A map[SystemID]bool dedup set trips the single-source guard, and should
- ANOMALY: `Model.Validate` used `map[agentic.SystemID]bool` to catch a model declaring one system twice. The guard reported it as a shadow binding table.
- DECISION: Rewritten as a slice scan rather than widening the guard to exempt set-shaped maps. A set of "systems this thing supports" is one refactor away from being a table of what each of them does, and the model list is a handful of entries.
- NOTE: Worth recording because the first instinct is to call it a false positive. The rule's bluntness is the reason it caught four spellings two reviews wrote against it.
- SCOPE: `pkg/vendorplugin/vendor.go`.
- STATUS: Resolved (TASK-260821-atcotl).

### 1830 — Two rules answering one question two different ways is a hole neither rule looks like
- FINDING: The `pkg/agentic` guard's switch rule carried two nets — structural (this value is an `ID()` call) and vocabulary (this literal is a known id) — while its `if`/comparison rule carried only the vocabulary net. Consequence: dispatch on an id nobody has declared yet was CAUGHT spelled `switch id` and ADMITTED spelled `if id == "opencode"`. An if-chain is not an exotic spelling; it is the first thing many people write.
- ANOMALY: The mutant matrix could not see it. Every if-chain mutant in the file used a KNOWN id, so the vocabulary net answered for all of them and the structural net was never asked — a full green matrix over a rule that was half missing.
- NOTE: Reusable lesson, independent of this guard: when two code paths are supposed to decide the same fact, they must call ONE helper, and each path needs at least one test whose input only the shared half can answer. Here that means undeclared ids in both spellings.
- FIX: Both rules now route through `dispatchesOnID`/`isIDExpr`; four permanent mutants with undeclared ids. NARROW-F6 (remove only the structural half) reds exactly those three comparison mutants and leaves the known-id if-chains green, which is the proof the nets are independent.
- STATUS: Resolved (TASK-260821-21vywo rev 3).

### 1829 — A residual declared with the wrong boundary is worse than an undeclared one
- FINDING: A review asked for `id := string(sys.ID()); switch strings.TrimSpace(id)` to be documented as a residual under the class "single-hop resolved, multi-hop declared open". That local is ONE hop. Filing it there would have put a false sentence into a file whose entire defect history is prose promising more than the rules deliver.
- DECISION: Closed it in code instead — the switch tag now looks through one-argument calls to a resolved local, which is what it already did for the identical tag WITHOUT the local. The resolver was not extended: `idCallLocals` is unchanged and still single-hop, because chasing assignment chains is the dataflow regress the source repository already refused to enter. The residual is now named by what the code actually keys on — the hop count, not the conversions around it.
- NOTE: A residual class is a claim about a mechanism. If the class name does not match the mechanism, a reader who trusts the list is misled more precisely than by no list at all.
- STATUS: Resolved (TASK-260821-21vywo rev 3).

### 1828 — "Can never happen" over a single read is a promise the code cannot pay
- FINDING: `System.ID`'s docstring said Register's normalization refusal meant a caller holding the plugin and a caller reading the registry "can never see two spellings of one system". `Register` read `ID()` once. A plugin whose `ID()` is unstable registered under its first answer, served its second to anyone holding it, and `BuildPlan` planned around the disagreement silently.
- FIX: Both halves, because either alone still lies. `Register` reads `ID()` twice and refuses a mismatch (`ErrUnstableSystemID`) — cheap, and it catches the accidental version: an id computed from mutable state or memoized on the wrong call. The promise was then rewritten to what the code backs: the registry key IS the spelling `ID()` returned at registration, for as long as the plugin keeps its word; "forever" is the plugin's obligation, not the registry's check.
- NOTE: The unenforceable half is a TEST, not a caveat in prose — `TestRegisterCannotSeeAnIDThatFlipsAfterRegistration` demonstrates a post-registration flip being admitted, so if a future change starts catching it, the test fails and the promise gets rewritten with it. Same pattern as the declared-open residuals in entry 1814.
- NOTE: Narrowing beats deleting here too. Comparing NORMALIZED values instead of raw ones admits exactly the case class the reviewer's probe walked through (`pangolin` → `Pangolin`), and reds the gate test — proof the gate covers the class, not just literal inequality.
- STATUS: Resolved (TASK-260821-21vywo rev 3).

### 1815 — A guard is worth what an outsider failed to defeat, not what its author tried
- FINDING: Revision 1 of the `pkg/agentic` single-source guard shipped 18 author-written mutants, all green. A reviewer wrote 15 spellings the author had not, and 4 walked straight through: an id copied into a local before the switch (`id := sys.ID(); switch id`), a table assembled key by key from an `init()`, a `const` declared inside a function body, and a value of a binding-table type the registry legitimately declares (`make(bindings)`).
- NOTE: None was obfuscation. Each is the ordinary way to write the thing — which is the class the guard exists for. The author's mutants clustered around the rules the author had just written; the reviewer's did not.
- FIX: Both sets now live in the suite permanently (`TestSingleSourceGuardCatchesShadowTables`, `...IDSwitches`, `TestSingleSourceGuardAgainstReviewMutants`), so the matrix in a review verdict is reproducible instead of a claim about a scratch copy.
- STATUS: Resolved (TASK-260821-21vywo rev 2).

### 1814 — Declared-open residuals are honest only because closing one turns a test red
- NOTE: `TestSingleSourceGuardResidualGaps` asserts that specific spellings are NOT reported. A review closed one of them (call-wrapped literals) with a four-line change to `foldStringConst`, and the residual test went red naming the case and telling the author to move it into the mutant set. The mechanism is demonstrated, not asserted.
- DECISION: Reusable pattern, independent of this task: a guard's threat model belongs in executable form. Prose saying "X is out of scope" drifts silently; a test that fails when X comes into scope cannot.
- NOTE: The same test now also carries a declared NON-goal (a switch on an accessor named `Identifier()` rather than `ID()`), so widening the guard to arbitrary identity-shaped method names has to be argued rather than happening.
- STATUS: Resolved (TASK-260821-21vywo rev 2).

### 1813 — A documented normalization contract with nothing enforcing it is two names for one system
- FINDING: `System.ID()` documented "must normalize to itself"; `Registry.Register` only checked that the id normalized SUCCESSFULLY. A plugin returning `"  PANGOLIN  "` registered fine — `IDs()` and `Plan.System` carried the folded spelling — while the plugin kept answering the raw one to anyone holding it from `Lookup`.
- FIX: `Register` refuses when `NormalizeSystemID(sys.ID()) != sys.ID()` (`ErrUnnormalizedSystemID`), naming both spellings.
- NOTE: This makes one earlier test's shape unreachable: "register `Pangolin`, expect `  PANGOLIN  ` to collide as a duplicate" cannot happen when neither spelling registers at all. The replacement asserts the stronger fact. The same applies to the CLI test that proved normalization by registering `Claude-Code` and watching `claude-code` print — it now proves the CLI can never surface a non-canonical spelling because such a plugin cannot be registered.
- NOTE: This is the source repo's two-normalizations disease caught one layer up: not two folding functions disagreeing, but one folding function applied on one side of a boundary only.
- STATUS: Resolved (TASK-260821-21vywo rev 2).

### 1812 — Single-source guard: structural rules outlive the id vocabulary
- DECISION: The `pkg/agentic` guard detects a shadow binding two ways — structurally (`map[SystemID]T` as a TYPE EXPRESSION anywhere outside `registry.go`; a `switch` on a value's `ID()`, including one copied into a local first), and by a local vocabulary of known system ids resolved through const/var/funclit indirection at package AND function-body scope.
- NOTE: The structural half is what matters. This module has no concrete plugins yet, so the vocabulary half has zero live matches today and would have looked healthy while guarding nothing. `map[SystemID]T` and a switch on `ID()` fire against ids nobody has declared — proven by mutants naming `opencode` / `some-future-harness`.
- AMENDED (rev 2): at revision 1 that claim was true only for the literal `switch sys.ID()` spelling. `id := sys.ID(); switch id { case "opencode": }` fired NOTHING — no structural rule, and no vocabulary entry either, so an undeclared id had no net at all. `idCallLocals` now resolves a local bound once to an `ID()` call and never rewritten; a rewritten variable and an id that leaves through a helper are declared-open residuals rather than approximated dataflow. See 1815.
- NOTE: Detecting the TYPE EXPRESSION rather than the composite literal is what catches `make(map[SystemID]T)`, a struct field, and a var declaration with no literal anywhere. Narrowing it to composite literals only (mutant M5) leaves four spellings undetected. The same lesson applied twice more in rev 2: a table filled in key by key (`t["codex"] = ...`) has no literal either — and that is exactly how the extraction source's own `adapterTable` is built, from an `init()`, because a var initializer there would have created an initialization cycle.
- SCOPE: `pkg/agentic/singlesource_guard_test.go`; 32 in-suite mutants across two sets in `singlesource_guard_mutants_test.go`.
- STATUS: Resolved (TASK-260821-21vywo rev 2).

### 1811 — A guard that scans a hardcoded directory list expires silently
- FINDING: The extraction source's argv guard names its two package directories explicitly. A third package added later is unguarded and nothing says so.
- FIX: `moduleSources` walks from `go.mod` and takes every non-test `.go` file. `TestSingleSourceGuardScansTheWholeModule` asserts named files were actually reached, so a clean result means "nothing found" rather than "nothing looked at".
- NOTE: The companion is `TestSingleSourceGuardRulesFireOnRealCode` — it rescans the real tree with the allowlist EMPTY and requires the registry's own map to be reported. Without it, green is equally consistent with a rule that never matches anything.
- STATUS: Resolved (TASK-260821-21vywo).

### 1810 — "Every dispatch surface" has to be read off the interface, not listed
- DECISION: `TestRegisteringOneSystemDrivesEveryDispatchSurface` enumerates `System`'s methods by reflection and requires the test double to have recorded a call on each after `BuildPlan` runs.
- NOTE: The source repo's axolotl test lists its five dispatch sites in a comment. That list is correct exactly once. A method added to the contract and not wired into `BuildPlan` fails the reflection version on the next run and cannot fail the hand-written one.
- SCOPE: `pkg/agentic/double_test.go`.
- STATUS: Resolved (TASK-260821-21vywo).

### 1809 — DryRunArgs was a seam, not a surface
- DECISION: The source adapter's `DryRunArgs` becomes `Argv(req, LaunchModeDryRun)` rather than a sibling method.
- NOTE: Its whole contract was "the same grammar as the real launch, minus side effects" — and the source paid for the gap: `BuildArgs` reported a hardcoded `codex`/`agy` while the real launch executed a managed-npm or preflighted binary. Two functions that must agree will eventually not. One function with a mode argument has nowhere to drift.
- SCOPE: `pkg/agentic/system.go` (`LaunchMode`), `TestDryRunMirrorsTheRealLaunchBinary`.
- STATUS: Resolved (TASK-260821-21vywo).

### 1808 — CompositionGrammar as a core enum contradicts the plugin model
- DECISION: `GrammarID` is an opaque string the plugin declares plus `System.ValidateComposition`, not a core enum with a core-side switch.
- NOTE: The source's enum meant a harness with a novel composition shape required editing core — the opposite of what a plugin seam is for. `GrammarNone` is still refused by `BuildPlan` BEFORE the plugin's validator runs, so a permissive validator cannot admit a composition the system declared no grammar for.
- STATUS: Resolved (TASK-260821-21vywo).

### 1807 — DefaultHome in the source adapter dispatched nothing
- FINDING: `Adapter.DefaultHome` was the literal string `"Config.ChildWorkDir()"` on all seven rows — documentation that `BuildCommand` overwrote `cmd.Dir` unconditionally after dispatch.
- DECISION: `Capabilities.DefaultHome` + `HomeEnvVar` now carry the harness CONFIG home, which is what `docs/architecture.md` names and what invariant 2's `IdentityKey(provider, home)` is keyed by. The child working directory became an explicit `LaunchRequest.WorkDir` / `Plan.WorkDir`.
- NOTE: Reusing the source's field name for a different fact would have been the worse option; the name is the same, the meaning is not, and it is called out in the task results.
- STATUS: Resolved (TASK-260821-21vywo).

### 1806 — The CLI's private plugin list WAS a second binding
- FINDING: `cmd.registeredPlugins []string` (scaffolded in TASK-260821-3svtog as a deliberate placeholder) is a second source for "which systems exist", which is exactly what this task's guard forbids.
- FIX: Deleted. `plugins` reads `agentic.Default` through a swappable `systemRegistry` var; the cmd test helper registers through the same public API a plugin's `init` would use.
- NOTE: Wiring the CLI was optional in the brief. Leaving it was not neutral — the placeholder would have survived as a shadow table the guard does not scan for by vocabulary (it holds no system ids today) and would have drifted the moment the first plugin landed.
- STATUS: Resolved (TASK-260821-21vywo).

### 1717 — git check-ignore is index-aware; a guard naming tracked files goes vacuous
- FINDING: `git check-ignore` never reports a TRACKED file as ignored, whatever `.gitignore` says. `--no-index` is the flag that defeats the index lookup and reports the pattern truth.
- ANOMALY: `TestBuildOutputIsIgnoredAndSourcesAreNot` named six source paths that are untracked only while a Change Request is in flight. On an UNTRACKED tree the anchor-drop mutant is RED; on the same tree with those files TRACKED it is GREEN. The guard against the 1706 defect would have expired at the moment the work it guards landed.
- NOTE: The failure class is a NEW file being silently swallowed, so a guard that only names files already in the index can never observe it. Assert at least one path that does not exist yet.
- FIX: `--no-index` on the `git check-ignore` invocation, `tools/agents-management/cmd/build_integration_test.go:236`; added the non-existent paths `tools/agents-management/cmd/future_command.go` and `pkg/agents-management/foo.go` to the not-ignored list.
- NOTE: The two halves of the fix are independently load-bearing against different mutants, not redundant. On a TRACKED tree with the anchor dropped: `--no-index` alone is RED, the non-existent paths alone are RED, neither alone is GREEN — but against a `pkg/agents-management/` ignore rule, `--no-index` with the old six-path list is GREEN and only the `pkg/` path catches it.
- SCOPE: `tools/agents-management/cmd/build_integration_test.go`. This file is the template later extraction tasks copy; the rationale is commented at the invocation so it is not simplified away.
- STATUS: Resolved (TASK-260821-3svtog, review rev1 blocking finding).

### 1706 — Unanchored .gitignore pattern hid the entire CLI source tree
- FINDING: `.gitignore:2` carried a bare `agents-management` pattern. Slash-free patterns match directories, so git ignored `tools/agents-management/` itself, not just the built binary.
- ANOMALY: Every gate stayed green against invisible files — `make build`, `make vet`, `go test ./...` all passed on a tree git would have carried as empty. Only `git status` showed nothing to add.
- FIX: Anchored both build-output patterns: `/agents-management` and `/tools/agents-management/agents-management` (`.gitignore:4-5`).
- SCOPE: `TestBuildOutputIsIgnoredAndSourcesAreNot` in `tools/agents-management/cmd/build_integration_test.go` asserts both directions — sources not ignored, binary ignored.
- NOTE: Caught before the first Change Request. Would have shipped a CR containing config and README changes only.
- STATUS: Resolved (TASK-260821-3svtog).

### 1705 — Go linker silently ignores a stale -X target
- FINDING: `go build -ldflags "-X <pkg>.Var=v"` naming a package or variable that does not exist produces no error and no warning; the binary just keeps its default.
- NOTE: Consequence — ldflags injection cannot be verified from inside `go test`, which is not built with those flags. The only observation point is running the real build and reading the binary's output.
- SCOPE: `TestMakeBuildInjectsVersionMetadata` invokes `make build` with sentinel VERSION/COMMIT/BUILD_DATE rather than duplicating the ldflags string, so it binds to the shipped Makefile. `TestBuildWithoutLdflagsReportsDefaults` is its narrowing partner against hardcoded sentinels.
- STATUS: Resolved (TASK-260821-3svtog).

### 1704 — One root Go module, not module-per-tool
- DECISION: Single module `github.com/relux-works/skill-agents-management` at the repository root; CLI `main` at `tools/agents-management`, shared packages to come under `pkg/`.
- NOTE: The extraction source (skill-project-management) splits every tool and every `pkg/` package into its own module wired by `replace`. That earns its keep there because `pkg/board`, `pkg/remoteconfig` and `pkg/providerlimits` have independent consumers that must not inherit the CLI's dependency set.
- NOTE: Here `pkg/` is empty until the plugin contracts exist, so the split would be `replace` bookkeeping around nothing. One module keeps `go vet ./...` and `go test ./...` honest repo-wide from one directory. Splitting later is additive.
- SCOPE: `go.mod`, `Makefile`, `spawn.worktree_isolation.validation.commands` in `task-board.config.json` (gate runs from the repo root, not from `tools/agents-management`).
- STATUS: Resolved (TASK-260821-3svtog).

### 1703 — make install copies rather than symlinks
- DECISION: `make install` copies the binary to `~/.local/bin/agents-management`.
- NOTE: A symlink into `tools/agents-management/agents-management` dangles the moment `make clean` runs. Matches the extraction source's `_install-cli`.
- STATUS: Resolved (TASK-260821-3svtog).
